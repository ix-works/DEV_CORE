---
applies_to: [s4_private]
---
# Bug-Checklist — Frontend (freestyle UI5 + OData V2)

> **Bug_Expert bunu kullanır** (ADR 0018). Her madde **must-do** — kozmetik buraya YAZILMAZ → **kanıtlı ihlal = ZORUNLU FİX** (builder takdiri değil). Kapsam = **diff + blast-radius**.
>
> **Automatable** sütunu G3 = `check_ui5_freestyle_traps.py` deterministik yakalar (LLM-yargısı değil) → Bug_Expert ÖNCE bunu koşar. Diğerleri semantik (kanıt-zorunlu inceleme).
>
> Kaynak: `../ui-freestyle-odata-v2.md` §K/§J · ADR 0008/0014/0017 · memory feedback'ler.

| ID | Kontrol (semptom) | Severity | Automatable | Ref |
|---|---|---|---|---|
| FE-01 | **V2 nav `_X`** (createEntry/$expand/path) — CDS composition adı; V2'de `to_X`. Semptom: Create kaydet sessiz boş / Change $expand boş | BLOCKER | **G3 T1** | §K2 |
| FE-02 | **Save = setProperty+submitChanges** (sıralı `oModel.update(merge)` değil). Semptom: programatik-set değer (auto-fill/cascade) kaydedilmiyor | HIGH | — | §K1 |
| FE-03 | **Eş-zamanlı `update`** aynı BO'da (sıralı zincir değil). Semptom: "Kaydedilemedi" (RAP BO kilit çakışması) | BLOCKER | — | §K1 |
| FE-04 | **MERGE'de boş tarih `""`** (null değil). Semptom: 400 Edm.DateTime | BLOCKER | — | §K1 |
| FE-05 | **`setData` eksik şekil** (`sel`/`hasSel`/`messages` düşmüş). Semptom: detay paneli seçmeden açık + seçince dolmaz | HIGH | — | §K3 |
| FE-06 | **`core:Title`** VBox/HBox/CSSGrid child (sap.m.Title değil). Semptom: view render crash "not valid for aggregation" | BLOCKER | **G3 T3** | §J |
| FE-07 | **`type=Number`** editable grid/miktar input (type=Text+liveChange değil). Filtre-sayaç istisna | WARNING | **G3 T2** | feedback_numeric-input-no-type-number |
| FE-08 | **Liste daralınca/yeniden sıralanınca SEÇİM BAYATLAR → YANLIŞ KAYIT SİLİNİR** (2026-07-29'da 7 varyantla genişletildi; eski hâli yalnız "tık yutulur" diyordu). `setData`/binding-refresh satırları **yeniden indeksler**: `sap.m.Table` `rememberSelections` **varsayılan true** → seçimi binding-context yolundan geri yükler; `sap.ui.table.Table` seçimi **indeks-bazlı** (`getSelectedIndex(es)`) ve kayıt `aList[iIdx]` ile okunur → A silinince **B seçili görünür** → sonraki Sil **YANLIŞ kaydı** siler (anında DB DELETE ise **geri alınamaz**). **Çözüm: temizliği çağrı yerlerine DAĞITMA, ortak GİRİŞ NOKTASINA al** (`_reload`/`_reloadX`/`_refreshList` ilk satırı) → gelecekte eklenecek çağrı da otomatik kapsanır (ölçüm: 7 dağıtık → 2 giriş noktası, net kod azalması). **Kontrol:** (a) listeyi yeniden kuran TÜM yollar (silme-başarı · silme-kısmi-fail · route-matched · onRefresh · search/clear · **auto-refresh** `refreshAfterChange` · **listeyi yeniden SIRALAYAN** filter+push akışları) → hepsi guard'lı giriş noktasından geçiyor mu; (b) **sınıf sınırı** — `Child._reload`'a koyup `Parent._reload`'a koymamak değişmezi böler; (c) giriş-guard'ı **o fonksiyondan GEÇMEYEN** yolu kapsamaz (modeli doğrudan güncelleyen başarı yolundaki temizlik **kaldırılmamalı** — asimetri gerçek); (d) **API**: `sap.ui.table`→`clearSelection()`, `sap.m.ListBase`→`removeSelections(**true**)` (`bAll` şart: hatırlanan yol listesini de siler); (e) ⛔ **`rowsUpdated`'a BAĞLAMA** — `sap.ui.table` onu `VerticalScroll`/`FirstVisibleRowChange`/`Resize`/`Zoom`/`Render` sebepleriyle de fırlatır → **scroll ederken seçim silinir**; (f) auto-refresh async → temizlik `remove()` sonrasına değil **`success` callback'ine**; (g) **item tablosu DIŞINI da tara** (atama/booking/konteyner tabloları). **Seçim anahtarı indeks/binding-path DEĞİL de belge-no ise** bu sınıf yapısal olarak yoktur (en kötü "kayıt yok" hatası) — ama `_loadData` girişinde seçim state'i (`_sSelectedX` **ve** `_oSelectedTable`) null'lanmalı. **Runtime kabul: `getSelectedItems().length`/`getSelectedIndices()` ile SAYIYLA doğrula, gözle değil.** | HIGH | (aday: G-sel — `_reload`/liste-yeniden-kurma fonksiyonlarında temizleme çağrısı varlık taraması) | §K4 · playbook/howto-delete-guard.md §7 · ZSD001 7 varyant 2026-07-29 (5 app item-tablosu · atama-tablosu kısmi-fail · liste-yeniden-sıralama) |
| FE-36 | **FE ön-kapısı KALDIRILMADAN önce backend karşılığı doğrulanmalı** (2026-07-29). FE'de "sil/kaydet'e basmayı engelleme" ön-kontrolü backend'i **kısa devre** yapar (BE kuralı ulaşılamaz kod olur), **bayat veriye** dayanır (sayaç liste yüklenirken okundu) ve mesajı ikiye böler. Kaldırmak doğrudur — **ama arkasında BE kuralı YOKSA kapı kaldırmak KORUMA SİLMEKTİR** → silme **sessizce başarılı** olur, bağlı kayıt yetim kalır. **Kontrol (dördü de EVET olmalı):** BE validation VAR mı · **doğru entity'de mi** (root vs child) · `on save { delete; }` ile **KABLOLU** mu (BDEF'ten, canlı `adt_get`) · mesajı **belge numarası** taşıyor mu. Biri HAYIR → **kapı KALIR**, önce BE kuralı. ⚠ **Kandırıcı komşu kural:** root'u koruyan validation child'ı korumaz; `on save { create; update; }` delete'i kapsamaz — "bu alanda bir validation var" YETMEZ. **KAPI DEĞİLDİR (kalmalı):** satır-seçim kontrolü · düzenleme-modu (`enabled="{ui>/editable}"`) · kilit (`!readOnly`) · veri-kaybı guard'ı (`_hasPendingChanges`) · gösterim affordance'ı. | BLOCKER | (aday: G-gate — FE `MessageBox+return` ön-kapısı ↔ BDEF validation eşleşme taraması) | playbook/howto-delete-guard.md §2-3 · ZSD001 voyage-hedef 2026-07-29 (kapı kaldırılacaktı, BE'de validation YOKTU → sessiz yetim referans) |
| FE-37 | **Model-only silme → ANINDA DB silmeye çevrilirken pending-detektör `onSave` MERGE payload'ıyla 1:1 olmalı** (2026-07-29). Dönüşüm yeni bir sözleşme açar: eskiden kullanıcı Kaydet'e basmadan çıkarsa **hiçbir şey olmamıştı**; artık silme **anında ve geri alınamaz** → silme öncesi "kaydedilmemiş değişiklik" guard'ı **TÜM düzenlenebilir alanları** kapsamalı, **BAŞLIK DAHİL**. Kapsamazsa: kullanıcı başlığı değiştirir → kalem siler → `_reload()` başlığı DB'den geri kurar → **düzenleme sessizce kaybolur** (tek geri bildirim başarı toast'ı). **Kontrol:** detektörün diff'lediği alan kümesi = `onSave`'in MERGE ettiği alan kümesi (başlık + kalem ayrı ayrı) + eklenen (key boş) + çıkarılmış (snapshot diff) kayıtlar. **App'ler arası KOPYALAMA YASAK** — her app'in KENDİ payload'ından ve KENDİ view'ındaki düzenlenebilir kontrollerden ölç (ölçüm: kardeş app'lerde 4+3 / 3+2 / 3+1 alan çıktı). **Kıyas tuzakları:** tarih için **epoch normalizasyonu** (`+new Date(v)`) şart — DatePicker `dateValue` **Date nesnesi** verir, ham `!==` eşit tarihte bile `true` der; boolean için `!!` (Edm.Boolean `undefined` vs `false`). **Yanlış-pozitif kontrolü:** `_populate` sonrası async yazıcılar diff'lenen alanlara yazıyorsa detektör HER silmeyi bloklar = fix'in kendisi bug. | HIGH | (aday: G-pend — pending-detektör alan kümesi ↔ onSave MERGE payload'ı diff) | playbook/howto-delete-guard.md §8 · ZSD001 ihr/sip 2026-07-29 (başlık kapsanmıyordu → sessiz kayıp) |
| FE-38 | **Çoklu `oModel.remove/update` PARALEL çalıştırılırsa kilit çakışır** (`useBatch:false` ise, 2026-07-29). `Promise.all(ops)` + `new Promise` executor'ı **senkron** → istekler push anında uçar; `useBatch:false` → changeset yok → **kısmi silme mümkün** ve paralellik **gerçek ağ paralelliği** = aynı BO'da kilit çakışması. → **Sıralı koşucu + thunk** (`function(done,fail){ ... }`). ⚠ **THUNK KAPANIŞ TUZAĞI:** build anında hesaplanan değerler thunk'ın **İÇİNE** alınırsa final değere kayar — vaka: `ItemNo: ("00000"+maxNo).slice(-6)` içeri alınsaydı birden çok yeni kayıt **AYNI anahtarla** create edilirdi. Değeri **build anında sabitle**; `ops` içinde kullanılan **her dış değişkeni** kapanış açısından izle. 📌 Bilinçli sonuç: sıralıda ilk hatadan sonraki op'lar çalışmaz (paralelde hepsi denenirdi) → kısmi-başarı sonrası **model↔DB resenkronu** (`_reload`) şart, yoksa retry aynı `remove`'u yeniden üretip **404 → save kalıcı kilitlenir**. | HIGH | (aday: G-par — `Promise.all` içinde OData change-request taraması) | playbook/howto-delete-guard.md §9 · ZSD001 fittings_se 2026-07-29 |
| FE-09 | **MERGE payload'da readonly `*Name`/`*Text`** (Edm hesaplanmış/assoc-path). Semptom: 400 / tüm MERGE reddedilir | HIGH | — | §K1 |
| FE-10 | **Plumbing sıfırdan yazılmış** (§K reuse yerine). Booking meta-dersi: çözülmüş mekaniği inferior desenle yeniden | HIGH | — | §K |
| FE-11 | **Referans edilen i18n key i18n'de tanımsız** — view `{i18n>k}` ve JS `_txt`/`getText("k")` literal'lerinin HER biri `i18n.properties` VE `i18n_tr.properties`'te tanımlı OLMALI. Pass/fail: kod-referans kümesi ⊆ tanım kümesi (iki dosyada da). Semptom: ham key ekranda (ör. "btn.cols") veya boş label. **Kör nokta:** kopyalanan util (TablePersonalizer'ın `_txt("var.*"/"btn.*"/"flt.*")` çağrıları) diff'te olmasa da kontrol edilmeli — çalışan app (booking) i18n'iyle çapraz-doğrula | WARNING | (aday: i18n-completeness validator) | §J · feedback_i18n-tr-her-iki-dosya · shipment "btn.cols" 2026-06-21 |
| FE-12 | **Recon kuralı dokümante, UI'a IMPLEMENTE değil** (gating/cascade). Semptom: kullanıcı "şu kural çalışmıyor" der | BLOCKER | — | feedback_done-tam-kapsam-dogrula |
| FE-13 | **Liste/rapor grid değil** (sap.ui.table + TablePersonalizer; **ADR 0008 = OTOMATİK-MUST**). m.Table YALNIZ mobil istisna | HIGH | `check_list_view_grid` (⚠️ ORPHAN — wire-aday) | feedback_grid-liste-standardi · ADR 0008 |
| FE-14 | **Audit alan auto-fill yok** (created/updated by-date-time, idempotent setAdmin) | HIGH | — | feedback_audit-alan-autofill-standardi |
| FE-15 | **Belge-kilidi / app-lock eksik veya yanlış** (read-only/heartbeat/beforeunload; ADR 0014) | HIGH | — | feedback_belge-kilidi |
| FE-16 | **Control API / nav-adı TAHMİN edilmiş** (canlı $metadata / ui5-mcp get_api_reference ile doğrulanmamış) | HIGH | — | §K2 / ADR 0017 |
| FE-17 | **"done" runtime-doğrulanmamış** (G1 smoke / en az G3 geçmemiş; node--check/XML-well-formed YETMEZ) | HIGH | — | ADR 0017 G4 |
| FE-18 | **EKSİK:** F4/VH alanında seçilen kodun **alt-tanımı (ad/açıklama) ekranda gösterilmiyor** (input + altında `<Text class="bookingFieldDesc">` deseni; başlık+detay her VH alanı). Kod çalışıyor → HATA değil, **standart eksik** | HIGH | — | feedback_belge-uygulama-ortak-ui-sablonu · §K |
| FE-19 | **EKSİK:** Ortak UI şablonu karşılanmamış (List filtre+akordion / başlık+kalem deseni; tüm belge-uygulamaları aynı layout) | MEDIUM | — | feedback_belge-uygulama-ortak-ui-sablonu |
| FE-20 | **Binding/MERGE-payload property adı canlı `$metadata`'da YOK** (eski alias / `ref_docs` CDS / **bayat `localService/mainService/metadata.xml`**'ten kopya → projeksiyon yeniden-adlandırılınca drift). FE-09'dan farkı: alan readonly değil, **hiç yok**. Semptom: başlık/diğer alanlar kaydolur ✅ / o alanı içeren kalem sessiz kaybolur veya "Kaydedilemedi" ❌ (SADL V2 deep-insert bilinmeyen property'yi SESSİZCE düşürür; MERGE'de 400). Kontrol: her save-payload + binding + picker-buffer property adını **aktif projeksiyon `$metadata`/adt_get** ile birebir doğrula — `localService/metadata.xml` ve `ref_docs` eski CDS bayat olabilir, GÜVENME | BLOCKER | — | ZSD001 booking `FinalAddress`→`FinalPartnerAddress` · sip_se `Charg`→`Batch` |
| FE-21 | **Generic hata yutma** — save/create/action error callback'i `that._t("msg.saveFail")` gibi sabit i18n gösterir, **gerçek `responseText` SAP mesajını parse ETMEZ**. Semptom: "Kaydedilemedi" çıkar ama kök-neden teşhis edilemez → kör iterasyon. Kontrol: her error callback `_parseError(oErr)` (responseText→`error.message.value`) ekleyip mesaja basmalı; createEntry/batch'te `__batchResponses`/`__changeResponses` ≥400 yanıtı da | HIGH | — | ZSD001 BaseOrder `_parseError` · feedback_done-tam-kapsam-dogrula |
| FE-22 | **Commit-barrier yok** — `onSave`/`onCreate` doğrudan `_save()` çağırır; `document.activeElement.blur()`→`setTimeout(_save,0)` sarmalayıcısı yok. Semptom: son düzenlenen Input/DatePicker JSON-model'e commit edilmeden gönderilir (değer kaybı / eski değer kaydolur) | HIGH | — | §1-B B7 · voyage Detail kanıtlı |
| FE-23 | **Paralel Create/Change controller'da fix-paritesi yok** (ortak base controller yerine kopya kod: save alan-listesi, cascade, VH map, clear-satırları). Semptom: bir bug birinde düzeltilir, ikiz controller'da kalır (`FinalAddress` İKİSİNDE de vardı). Kontrol: bir controller'da bulgu → ikiz controller'da aynı satırı doğrula | MEDIUM | — | ZSD001 BaseOrder paylaşılan-base dersi |
| FE-24 | **Kaydetme başarı GERİBİLDİRİMİ yok/kaybolur** — save success'te `MessageToast.show()` + **hemen** `navTo` → toast sayfa geçişinde KAYBOLUR (kullanıcı "mesaj gelmedi" der). Belge-no'suz generic "Kaydedildi" de zayıf. Kontrol: save success = **`MessageBox.success(belge-no'lu metin, { onClose: navTo })`** (modal, garantili görünür, navigasyon OK sonrası — ZSD001 stabil deseni). Her CRUD action'ında net geribildirim olmalı | HIGH | — | standards/03 §save-feedback · ZSD001 CreateOrder · voyage 2026-06-17 |
| FE-26 | **Manifest JSONModel `settings.data` ÇİFT-SARMALAMA** — manifest'te isimli JSONModel başlangıç verisi `settings` objesinin DOĞRUDAN içine yazılır (`"settings": { "busy": false, "filter": {} }`). `settings.data` ALTINA koyarsan (`"settings": { "data": {...} }`) UI5 tüm `settings` objesini constructor'a data olarak verir → veri `/data/...` altına düşer, **tüm `{model>/...}` kök-binding'leri (busy/filter/editable/locked vb.) SESSİZCE undefined** olur. Semptom: F4 seçimi yansımıyor (FE-25 görünümü), busy spinner hiç dönmüyor, filtre tutmuyor, read-only/lock state binding'i ölü. Kontrol: her isimli JSONModel için manifest `settings` içinde `data` anahtarı VAR MI + app `{model>/data/...}` kullanıyor mu? `/data/` referansı YOK ama `settings.data` VAR = bug (controller `setData/setProperty` ile maskelemiyorsa). Kanıt: `ui.getData()` runtime + `setProperty('/path')`→false | HIGH | (aday: manifest JSONModel `settings.data` taraması) | voyage F4 2026-06-18 (kök-neden) · sweep: 9 kardeş app |
| FE-27 | **OData V2 `Edm.DateTime`/`DateTimeOffset` alanına create/update payload'unda STRING geçme** → **JS Date objesi** olmalı (V2 model `/Date(epoch)/`'e serialize eder; düz string geçince backend deserialize edemez). Semptom: kaydet'te `Conversion error for property '<X>' at offset N`. **FE-04'ten farkı:** FE-04 = MERGE'de **boş** tarih `""`; FE-27 = **dolu ama string-tipli** (create/deep-insert dahil). Kök tuzak: `DatePicker value="{...}" valueFormat="yyyy-MM-dd"` modeli STRING tutar → payload'da `new Date(Date.UTC(y,m-1,d))`'e çevir (TZ off-by-one'a karşı UTC-parça), VEYA DatePicker'ı `dateValue=` (Date objesi) ile bağla. Kontrol: her save/create payload'undaki her Edm.DateTime property → Date objesi mi + **Create ↔ Change paritesi** (biri `dateValue` doğru, ikiz `value`-string kırık olabilir — FE-23 ile bağlı) | BLOCKER | (aday: DatePicker `value=`+`valueFormat`→payload-string taraması) | ZSD001 sip_se OrderDate 2026-06-19 (Create string→Conversion error; Change `dateValue` sağlamdı) |
| FE-28 | **Save/create/update payload property Edm-tipi CANLI $metadata ile EŞLEŞMİYOR** (genel kontrol; FE-27 = DateTime özel-hali). HER payload alanı canlı Edm-tipinde gönderilmeli: **Boolean** alan → `true/false` (DEĞİL `'X'/''` string), **Decimal** → packed→string direkt (`WRITE...TO` yok), **DateTime** → Date objesi. **KRİTİK: `localService/.../metadata.xml` mock BAYAT olabilir (FE-20) → CANLI `$metadata`/adt_get YETKİLİ, mock'a GÜVENME.** READ-tarafı da: Boolean alanı `=== 'X'` varsayan formatter/binding (ObjectStatus/Switch/CheckBox) boolean'a göre olmalı. Semptom: kaydet'te `Failed to read property '<X>' at offset N` (Boolean'a string) veya `Conversion error` (DateTime). Kontrol: bug-expert her Create+Change save-payload property'sini **canlı $metadata Edm-tipiyle birebir** eşler (alan-alan tablo); mock-kopya kabul edilmez. Create↔Change paritesi (FE-23) | BLOCKER | (aday: payload-property → canlı-$metadata Edm-tip diff validator) | ZSD001 sip_se Blocked 2026-06-19 (Edm.Boolean'a `'X'` → Failed to read; mock String diyordu) |
| FE-25 | **VH/F4 seçimi alana DEVRALINMIYOR** (dialog açılır+liste gelir, ama seçilen kayıt input'a/modele yazılmaz). FE-18'den farkı: FE-18 = ad GÖSTERİLMİYOR (kod çalışıyor); FE-25 = değer hiç YAZILMIYOR (kod yanlış). **Statik 3-nokta write-back izi** (her `valueHelpRequest=".onF4X"` Input için): (1) Input value-hedefi = `{model>/path}` + binding-context, (2) `.onF4X`→SelectDialog `confirm`→`setProperty(...)` üçlüsü, (3) **birebir eşleşmeli**. İhlal: yanlış model (Input "v"/default ↔ confirm "ui"), yanlış path, context uyumsuz (mutlak path ama Input satır-aggregation'ında / tersi), confirm `selectedItem` okumuyor/hiç yazmıyor, **yalnız `oInput.setValue()`'a güvenip** modele yazmamak (OneWay refresh siler), ya da item template `title`/`description` VH entity'sinde olmayan property'ye bağlı (`getTitle()` boş). Çalışan referansla (örn. Detail `_openVH`) kıyasla — fark = bug | HIGH | — | feedback_belge-uygulama-ortak-ui-sablonu · voyage filtre F4 2026-06-18 |
| FE-29 | **Save akışı callback-garantisiz → busy SONSUZ döner / kaydet bitmez** — `onSave` kendi `aUpdates` dizisi + custom `fnNext` özyinelemesiyle `oModel.update(merge)` çağrılarını **eager `createEntry`/`remove` + sonda tek `submitChanges`** ile KARIŞTIRIR (kanıtlı `_runSeq` sıralı-runner yerine), ve/veya busy'yi `getView().setBusy()` ile callback içinde aç/kapatır. Semptom: Kaydet'e basınca **busy spinner sonsuz döner, ne success ne error** — bir callback yolu hiç ateşlenmez / pending-changes flush edilmez / busy açık kalır. FE-02 (programatik-değer kaybı) ve FE-03 (kilit çakışması)'nın **somut "hiç bitmeyen save" hali**; ayrıca FE-10 (plumbing sıfırdan) meta-dersinin doğrudan sonucu. **Doğru desen (sip_se/ihr_se kanıtlı §K):** `ui>/busy` flag → header MERGE entity-path (`oModel.createKey`) `{merge:true,success,error}` → success içinde child persist-diff'i **`_runSeq(aSaves, doneCb, errCb)`** ile SIRALI (her adım `function(done,fail){...success:done,error:fail}` = GARANTİLİ tamamlanır) → child GÜNCELLE/SİL **entity-path** (`createKey`, KEY gövdede değil), child EKLE **nav-path** (`oModel.create(sPath+"/to_X")`) → kısmi-fail'de `_reload()` resync. Kontrol: save akışı kanonik `_runSeq` desenini mi kullanıyor, yoksa ad-hoc `update`-loop+`submitChanges` karışımı mı? Her olası yol (0 kalem / sadece-update / sadece-create / karışık) success VEYA error'a ULAŞIYOR mu? | BLOCKER | — | §K1 · sip_se ChangeSe `_runSeq` · ZSD001 booking 2026-06-20 |
| FE-30 | **Client-only `_`-property OData V2 entity context'e `setProperty` ile stamp** (havuz-merge sonucu, ör. `_miktarPak`/`_custMaterial`) → entity `mChangedEntities`'e pending-change girer + `hasPendingChanges()` true döner. Pass/fail: read-only liste için GÜVENLİ (app `submitChanges`/`createEntry` kullanmıyorsa) AMA aynı OData model bunları kullanıyorsa bilinmeyen `_x` property backend'e sızar → deep-insert/MERGE 400. Kontrol: OData context'e `_`-prefix `setProperty` var mı + aynı model `submitChanges`/`createEntry` çağırıyor mu? Varsa client-only stamp **parallel JSONModel'e (path-keyed)** yazılmalı, OData context'e DEĞİL | MEDIUM | (aday: OData-context `_`-property setProperty + submitChanges taraması) | ZSD001 shipment ③ `_miktarPak`/`_custMaterial` 2026-06-21 (bg-pak latent) |
| FE-31 | **VH/F4 dialog açılır ama liste BOŞ — VH entity-set'i raporun SRVD servisinde EXPOSE EDİLMEMİŞ** (canlı `$metadata`'da EntitySet yok). FE-25'ten farkı: FE-25 = liste GELİR ama seçim yazılmaz (FE kodu yanlış); FE-31 = liste HİÇ GELMEZ çünkü `SelectDialog items:{path:"/ZSD000_I_X_VH"}` bind ettiği entity-set servis metadata'sında YOK → binding sessiz boş. FE-20'den farkı: FE-20 = save-payload property yok; FE-31 = tüm VH entity-set'i yok. **Kök = BACKEND config** (FE doğru olabilir): generic VH rename/sweep sonrası bir tüketici raporun SRVD'sine `expose` eklenmesi atlanır. Kontrol: FE'nin bind ettiği HER VH entity-set'i (`_openFilterVH`/`_openVH`/cfg `set:`) → raporun SRVD'sinde `expose <VH>;` VAR MI (adt_get srvd) + canlı `/sap/opu/odata/sap/<SRVB>/$metadata`'da EntitySet olarak görünüyor mu. Generic VH (ZSD000_I_*) değişince TÜM tüketici servisleri tara (tek-yer-düzelt eksik; [[feedback_fix-oncesi-where-used-blast-radius]]). Düzeltme additive: SRVD'ye expose ekle + aktive + SRVB republish (FE/redeploy gerekmez) | HIGH | (aday: FE VH `set:` → tüketici SRVD expose + canlı $metadata EntitySet diff) | ZSD001 container_report ShiptoPartner/ShippingPartner (CUSTOMER_VH/VENDOR_VH expose eksik) 2026-06-22; kök = 2026-06-17 BPNAME→CUSTOMER/VENDOR sweep |

| FE-32 | **Rapor/liste filtre+VH+grid arama deseni** (SELECT-OPTIONS standardı, 2026-06-24). **(a) HATA·BLOCKER:** filtre/VH/grid arama Filter'ında `caseSensitive:false` KULLANMA → UI5 V2 `$filter`'da `toupper()`/`tolower()` üretir, SAP Gateway (/IWBEP) bunları DESTEKLEMEZ → **HTTP 400 "Function toupper/tolower is not supported"** (SAP Note 1797736) → arama hiç sonuç döndürmez. Düz `new Filter(path, FilterOperator.Contains, q)` (caseSensitive parametresi VERME) → düz `substringof` zaten HARF-DUYARSIZ (DB collation; canlı: "gül"→"GÜLAK" 200). **(b) EKSİK·WARNING:** rapor filtre ekranı tek-değer `<Input>` değil **`<MultiInput>` + ValueHelpDialog (çoklu-değer + aralık)** olmalı; **(c)** wildcard `_parseSearchTerm` (`*x*`/`x`→Contains, `x*`→StartsWith, `*x`→EndsWith; startswith/endswith /IWBEP'te DESTEKLENİR, toupper DEĞİL); **(d)** serbest-metin/kod alanlarında düz-token default `Contains` (`defaultOp`); **(e)** grid sütun-başlığı filtresi (`TablePersonalizer._onColumnFilter`) string kolonda düz Contains (caseSensitive YOK) + wildcard. **Kanonik referans: ZSD001 sales_order_report.** | BLOCKER | `check_filter_search_pattern.py` | standards/03 §filtre-arama · playbook/ui-freestyle-odata-v2.md · ZSD001 2026-06-24 (caseSensitive:false→/IWBEP 400) |
| FE-34 | **F4/value-help alanında ELLE girilen değer VH'de yoksa doğrulanmıyor + kayıt bloklanmıyor** (İŞ KURALI, 2026-07-02). VH ile doldurulabilen bir alana kullanıcı elle/yapıştırma değer girebilir; girilen değer VH'nin geçerli değerleri arasında YOKSA: **(a)** Input `setValueState("Error")` + açıklayıcı `valueStateText`; **(b)** create/save **BLOKLANMALI** (geçerli değer seçilene/girilene veya alan boşaltılana kadar). Geçerli olma yolları (VH'de VAR / F4 seçimi / boş / başarılı-create) → `valueState("None")` + invalid-flag temizlenir. Amaç: geçersiz kod/referansla veri bütünlüğü ihlali önlenir — backend reddine bel bağlamadan kullanıcıya anında geri bildirim. **Güvenilir save-guard = veri-modeli flag'i** (`<X>Invalid`), yalnız valueState'e güvenme (çok-satır/context geçişinde kaybolur). Kontrol: her VH-bağlı Input (`valueHelpRequest`/`showValueHelp`) için change-handler değeri VH-entity'sine karşı doğruluyor mu + save/create handler geçersiz-flag'i denetleyip bloklyor mu. Kanıt: geçersiz elle-giriş → valueState Error + create/save çağrısı YAPILMIYOR (Playwright). | BLOCKER | (aday: VH-input change-validation + save-guard varlık taraması) | standards/03 §f4-manuel-giris-validasyonu · ZSD001 şoför Kimlik No 2026-07-02 (geçersiz elle-giriş kayda izin veriyordu) |
| FE-33 | **`sap.ui.layout.form.Form`/`SimpleForm` + `ColumnLayout`(veya ResponsiveGridLayout) içinde `f:fields`/form-content'e HBox/VBox/FlexBox/Panel gibi LAYOUT-CONTAINER konma** — form-content yalnız geçerli form kontrolü kabul eder (Input/Button/Text/ComboBox/CheckBox...); container = GEÇERSİZ. Semptom: **diyalog/view HİÇ AÇILMAZ** (kullanıcı "buton çalışmıyor" der), konsol: *"Element sap.m.HBox is not a valid Form content! Only use valid content in ...ColumnLayout"*. **KÖR NOKTA (bu bug tam buradan kaçtı):** aynı HBox-deseni **CSSGrid/VBox**'ta GEÇERLİ (ör. booking) → başka app'ten kopyalanan HBox+FlexItemData düzenini Form/ColumnLayout'a taşımak crash yapar. Layout-container tipini alan-yerleşimine göre doğrula. Çoklu-kontrolü tek hücrede: HBox yerine **doğrudan sibling `f:fields`** (Input+Button+Text) veya **ayrı `f:FormElement`** satırı. **Kontrol:** her `f:Form`/`SimpleForm` içindeki `f:fields`/content çocuğu container mı (HBox/VBox/FlexBox/Panel) → BLOCKER. **node--check/XML-well-formed BUNU YAKALAMAZ** (geçerli XML ama geçersiz UI5 aggregation → yalnız runtime render'da patlar) → **runtime açılış doğrulaması ZORUNLU (FE-17: diyaloğu AÇ, konsol temiz mi).** | BLOCKER | (aday: G3 — `f:Form`/ColumnLayout içi HBox/VBox/FlexBox içerik taraması) | ZSD001 shipment NewBooking/ContainerAdd 2026-07-02 (HBox in ColumnLayout → diyalog açılmadı; bug-gate kaçırdı çünkü XML-valid + render-doğrulanmadı) |
| FE-35 | **Miktar/quantity alanı HAM binding — birim-ondalık formatter'sız → OData QUAN "14.000" gösterir ("14" yerine)** (2026-07-05). OData V2 QUAN property'si (ADT/ADET gibi 0-ondalıklı birim dahil) FE'ye ham `abap.dec(x,3)` string olarak gelir; `<Text text="{se>/activeItem/DeliveredQty} {se>/activeItem/SalesUnit}"/>` gibi DÜZ-CONCAT binding trailing-zero'ları OLDUĞU GİBİ basar → "14.000 ADT". **Kör nokta:** aynı app'in GRID/tablo hücreleri formatter kullanır (doğru) ama sağdaki KALEM-DETAY paneli ham-concat kalır (kopyalanırken atlanır) → tablo "14", detay "14.000". Kontrol: her miktar `<Text>`/`<Label>` binding'i (Qty/Miktar/Bakiye/Remaining/Delivered) formatter kullanıyor mu (`.formatter.qtyPak` veya eşdeğeri; `Math.round(q*1000)/1000` + `String()` trailing-zero kırar) yoksa düz-concat mı. Düz-concat = EKSİK. Düzeltme: `{ parts:['...Qty','...SalesUnit'], formatter:'.formatter.qtyPak' }` (PAK istenmiyorsa vFactor GEÇME). Kanonik: ihr_se ChangeSe detay. | MEDIUM | (aday: G-qty — miktar-binding formatter'sız düz-concat taraması) | ZSD001 fih/dsk/fittings_se kalem-detay 2026-07-05 ("14.000 ADT"; tablo doğru, detay ham) |

| FE-39 | **Kapsam-dışı İNFRA değişikliği:** (BE-66 ile aynı kontrol, FE diff'leri için) görev-diff'inde paylaşılan-altyapı dosyası değişimi → howto-infra-fix EXPRESS-kriter/KUYRUK uygunluğu + ⚠GEVŞETME-bayrağı denetimi. | HIGH | — | howto-infra-fix-proseduru (2026-08-01) |

> **TİP:** Her madde **HATA** (kod yanlış) veya **EKSİK** (kod çalışıyor ama must-do/UX standardı karşılanmamış) tipindedir → Bug_Expert raporda `[HATA]`/`[EKSİK]` etiketler, ikisi de **zorunlu** (pass geçilmez). Çoğu FE-01..09 + FE-20/22/25/26/27/28/29/31/33/34 = HATA (FE-06/FE-33 = view/diyalog render-crash; FE-34 = geçersiz-giriş save-guard); FE-13/14/15/18/19/21/23 = EKSİK (UX/completeness/diagnosability). Checklist-DIŞI fikir = `[ÖNERİ]` (bağlayıcı değil).
> **Pre-existing AYRI:** değişimle ilgisiz görülen kritik = ayrı işaretle, bu değişimi bloklama.

| FE-40 | **Rapor sayıları içerik-çapalı mı?** (BE-70'in FE eşi) — nicel iddialar içerik çapasıyla mı? Çıplak sayı = EKSİK. | MEDIUM | — | denetim 2026-07-31 R6 · kimlik 2026-09-13'e kadar **FE-36** idi (yukarıdaki FE-36 ile ÇİFT tanım, Q270) |
| FE-41 | **0-sonuç iddiaları kontrol-gruplu mu?** (BE-64'ün FE eşi.) | MEDIUM | — | denetim 2026-07-31 R6 · kimlik 2026-09-13'e kadar **FE-37** idi (yukarıdaki FE-37 ile ÇİFT tanım, Q270) |

| FE-42 | **OData V2 `callFunction` BÜYÜK YÜK taşıyor → HTTP 414 (Request-URI Too Long), taşıma katmanında** (2026-09-16). V2 `urlParameters`'ı **URL'e** serileştirir (gövde ancak 5. argümanla doğar); dosya/base64/`JSON.stringify(dizi)`/uzun metin taşıyan function import **istek satırını** şişirir ve ters proxy duvarına çarpar. Duvarın sahibi **SAP Web Dispatcher**'dır — 414 gövdesindeki `<!-- This error page was generated by SAP Web Dispatcher (-)! -->` ile kanıtlanır (ICM DEĞİL; `icm/HTTP/max_request_size_KB` **gövde** sınırıdır, ilgisiz — oraya bakmak tur kaybettirir). Ölçülen eşik (tek kurulum): **8.398 B geçti / 8.498 B → 414**. **Semptom SATIR SAYISIYLA DOĞRUSAL ve SİNSİ:** az satırla çalışır, eşiği geçen ilk kayıtta patlar (ölçülen bir ekranda ~437 B/satır ⇒ **~20 satır**; sabit URL tabanı düşüldüğünde daha da az). **Kontrol:** her `callFunction` çağrı yerinde parametrelerin **üst sınırı** var mı — `JSON.stringify(<dizi>)` · base64 · serbest metin = **sınırsız** ⇒ bulgu. Sabit sayıda kısa alan = temiz. **Düzeltme:** o TEK çağrı için `useBatch:true` ikinci model (`manifest.json` DEĞİŞMEZ — §18.5a); dış URL sabit kalır, yük gövdeye iner (ölçülen tavan ≥512 KB iç istek satırı, ~60×). ⛔ **Duvarı yükseltme** (log'a dosya içeriği düşer; RISE/PCE'de müşteride değil; sınır kalkmaz ötelenir). ⛔ **DENENDİ-ÇALIŞMIYOR:** `application/x-www-form-urlencoded` gövde → Gateway **HTTP 200** döner ama gövdeyi **sessizce yok sayar** ⇒ 200 gördün diye geçme, **gövdeyi aç**. ⛔ Ham batch gövdesinde **kapanış ayracından önceki boş satır ZORUNLU**, yoksa `400 malformed syntax` ve mesaj boyutu işaret etmez. Bir 414 bulduğunda **tüm `callFunction` sitelerini tara** (kardeş vaka kuralı) | HIGH | (aday: `callFunction` parametrelerinde sınırsız-uzunluk kaynağı — `JSON.stringify`/base64 — taraması) | standards/03 §18.5a · ZSD001 açılış-lotu Excel yüklemesi 2026-09-16 (9.309 B dosya → 13.095 B istek satırı) |
| FE-43 | **İkinci `ODataModel` kuruldu ama `metadataUrlParams` DEVRALINMADI → metadata isteği patlarsa KALICI busy** (FE-42'nin düzeltmesinin yan etkisi; her ikinci model için geçerli, 2026-09-16). `ODataModel` **iki ayrı havuz** kullanır (`ODataModel.js:427-428`): `oServiceData` (CSRF) **`sServiceUrl`** başına paylaşılır, `oMetadata` ise **`sMetadataUrl`** başına. manifest'ten kurulan modele UI5 **kendisi** `mMetadataUrlParams['sap-language']` ekler (`Component.js:1909-1923`); `new ODataModel(...)` ile kurulan model **almaz** ⇒ anahtarlar **zorunlu olarak ayrışır** ⇒ ikinci modelin kendi `$metadata` GET'i doğar. Asıl bedel o ek istek değil: `ODataMetadata.js:87-95` yükleme **hata alırsa** `loaded()` **hiç settle olmayan** promise döndürür (yalnız sonraki BAŞARILI yükleme çözer) ve `ODataModel.js:5095` her isteği ona asar — **`.catch` yok, `timeout` yok** ⇒ istek hiç gitmez, `success`/`error` **hiç koşmaz** ⇒ busy bayrağı `false`'a dönmez ⇒ **sayfa ömrü boyunca kalıcı meşgul**, çıkış F5. **Kontrol:** `new ODataModel(` her çağrı yerinde `metadataUrlParams` devralınmış mı (`oMain.mMetadataUrlParams` — `:361`'de örnekte durur, **literal dil yazmaya gerek YOK**). Devralınmışsa `ODataModel.js:446-449` ana modelin **zaten çözülmüş** metadata'sını yeniden kullanır ⇒ hem ek istek hem asılma yolu yok olur. Devralmıyorsan busy'yi **zaman aşımıyla kurtar** + kullanıcıya söyle. ⛔ **Kaynak okuması bu soruyu tek başına CEVAPLAMAZ:** aynı vakada *"metadata paylaşılıyor, sorun yok"* hükmü kaynaktan verildi ve **canlı ağ izi çürüttü** — paylaşılan `oServiceData`'ydı. Kanıt **ağ izinden** gelir: ikinci `$metadata` GET'i var mı + `a.oMetadata === b.oMetadata` | MEDIUM | (aday: `new ODataModel(` → `metadataUrlParams` devri taraması) | standards/03 §18.5a · ZSD001 açılış-lotu yükleme modeli 2026-09-16 |
| FE-44 | **İstemci-tarafı dosya/yük BOYUT KAPISI yok ya da sınır İKİ AYRI LİTERALDE yaşıyor** (2026-09-16). Dosya yükleyen ya da büyük yük gönderen her ekranda istek **doğmadan önce** reddeden bir kapı olmalı; yoksa kullanıcı sunucu hatasıyla (414/413/timeout) karşılaşır ve ne olduğunu anlamaz. ⛔ **`FileUploader.maximumFileSize` FALSY değerde SESSİZCE ATLAR** (`FileUploader.js:1799,1806` — `fMaxSize = this.getMaximumFileSize(); if (fMaxSize && (fSize > fMaxSize))`; property `type:"float"`, `defaultValue:null`) ⇒ binding çözülmezse/model yolu yazılmazsa kapı **yok olur ve hiçbir şey söylemez**. Birimi **MB**'dir (bayt değil) — birim karıştırma kapıyı 1024× gevşetir ya da sıkar. **Kontrol:** (a) boyut sınırı var mı (b) sınır **TEK sabitten** mi besleniyor — kodun kullandığı eşik ile kullanıcıya gösterilen sayı ve `maximumFileSize` aynı kaynaktan mı geliyor, yoksa iki ayrı literal mi (ayrışırsa kullanıcıya yalan söylenir) (c) `maximumFileSize` bağlandığı model yolu **diyalog açılmadan ÖNCE ve HER açılışta** yazılıyor mu (tembel fragment + `destroy` edilen diyalogda ilk açılış tuzağı) (d) `fileSizeExceed` olayı **bağlı mı** ve tamponu temizliyor mu (e) reddedilen dosya için `FileReader` **kurulmuyor** mu — kuruluyorsa asenkron okuma sonradan dönüp "okundu" bayrağını `true` yapar ve kapı delinir. **Kanıt:** sınır üstü dosya seçildiğinde **ağda tek istek yok** + kullanıcıya birimi doğru mesaj + sınır altı dosya normal akıyor. FE-42'nin ön yüz ayağıdır (FE-42 = yükün kendisi 414 veriyor); FE-16 (control API tahmin edilmez) ve FE-17 (runtime açılış doğrulaması) bu maddede birlikte koşar | MEDIUM | (aday: `FileUploader` → `maximumFileSize` varlığı + sınırın tek-sabit izi) | standards/03 §18.5a · ZSD001 açılış-lotu yükleme diyaloğu 2026-09-16 (316.509 B istek doğmadan reddedildi; 262.144 B eşik tek sabitten) |
| FE-45 | **Tarih-yalnız (`Edm.DateTime` + `sap:display-format="Date"`, arkada DATS) alana/parametreye YEREL gece-yarısı gönderme → GÜN KAYMASI** — `DatePicker`/`DateRangeSelection` yerel `00:00` verir, V2 `ODataUtils` `Edm.DateTime`'ı UTC serialize eder, SAP gateway gelen UTC değerin **GÜN kısmını** alır ⇒ UTC'nin doğusunda (örn. UTC+3) değer `...(G-1)T21:00:00` olarak gider = **bir önceki gün**. Etki yüzeyleri: ① `$filter` sınırları (seçilen aralığa önceki günün kayıtları girer / seçilen gün düşer) ② function-import `urlParameters` ve create/update payload'u (yanlış gün **KALICI YAZILIR** — sessiz veri bozulması). ⛔ Yaygın *"alt sınır gün-başı, üst sınır gün-sonu 23:59:59"* reçetesi YANLIŞTIR: yalnız üst sınırı kurtarır, alt sınırı bir gün geri kaydırır. **Doğrusu:** seçilen takvim günü UTC gece-yarısı — `new Date(Date.UTC(d.getFullYear(), d.getMonth(), d.getDate()))`, iki sınır + tek-taraflı GE/LE + her tarih parametresi; yardımcı app'in BaseController'ında TEK kopya. **FE-27'den farkı:** FE-27 = payload'da STRING tip; FE-45 = Date objesi doğru tipte ama YANLIŞ ANDA (yerel gece-yarısı). Kontrol: (a) canlı `$metadata`'da `display-format="Date"` taşıyan her alan/parametre için FE kodunda `getDateValue()`/`setHours(`/`new Filter(<tarih>` izini sür — **kardeş taraması `urlParameters` + create/update payload'unu da kapsar, yalnız `new Filter` değil** (ilk vakada rapor ekranı ve iki function import ilk düzeltme turunda kaçtı); (b) kanıt = kontrol grubu: kaydı OLAN günü ve ERTESİ günü seç → ertesi gün 0 dönmeli (`$count` ile `...T00:00:00` ↔ `...(G-1)T21:00:00` karşılaştırması). İstemci-tarafı JSON süzgeci (OData'ya gitmeyen) bu tuzağa tabi değildir; UTC gece-yarısı GELEN değeri süzüyorsa aynı `utcDay` ile karşılaştır (her saat diliminde doğru) | HIGH | (aday: `display-format=Date` alanına giden `setHours(`/ham `getDateValue()` taraması) | S/4 private proje, freestyle V2 iki app 2026-09-23 (canlı `$count`: 23.09 seçimi 22.09'un 7 kaydını getirdi; `ge/le ...22T00` → 7, `...21T21:00` → 0; function-import tarihinin kaydı gateway GET kontrol grubuyla çıkarıldı, POST halkası ÖLÇÜLMEDİ) |
| FE-46 | **UI5 bootstrap'ı dış CDN'e sabitlenmiş patch'ten yükleniyor → patch silinince SESSİZ İngilizce tarih** — `index.html` `src="https://ui5.sap.com/<sürüm>/resources/sap-ui-core.js"`. Bakım dışı patch `versionoverview.json`'da `removed: true` olur ve CDN'den silinir; silme **yarım** olabilir: `sap-ui-core.js` 200 ama `cldr/<dil>.json` 404 ⇒ UI5 hata vermeden `en`'e düşer, tarih/ay adları İngilizce basılır ("Sep 21, 2026"). FLP etkilenmez (backend UI5'i) ⇒ yalnız doğrudan BSP URL + lokal testte görünür, canlı kullanıcı çoğu zaman görmez. Göreli `resources/sap-ui-core.js` de YANLIŞ (BSP altında çözülmez). **Doğrusu:** `src="/sap/public/bc/ui5_ui5/resources/sap-ui-core.js"` (backend'in kendi UI5'i; lokalde `ui5.yaml` `/sap` proxy'si taşır) — std 03 §2.5. Kontrol: (a) diff'teki/uygulamadaki her `index.html` `src`'si (b) runtime: `sap.ui.version` = backend sürümü + `…/cldr/<proje-dili>.json` **200** + bir tarih alanı proje dilinde. ⚠ `ui5.yaml` `ui5:` proxy ayarını değiştirmek bunu ÇÖZMEZ — mutlak CDN adresi proxy'ye hiç uğramaz. | MEDIUM | ✅ grep: `index.html` içinde `ui5.sap.com/` ya da çıplak `src="resources/` | std 03 §2.5 · lessons-learned PATTERN #37 · S/4 private proje 2026-09-23 (18 app; CDN 1.120.23 `tr.json` 404, `de`/`en` 200) |
| FE-47 | **Function import `urlParameters`'ına `null` değer verildi → 400 "Invalid key predicate"** — V2 `callFunction` `null`'u atlamaz, URL'ye `Param=null` yazar; gateway bunu literal olarak ayrıştıramaz ve `/IWCOR/CL_DS_URI` 400 döner (mesaj anahtar/predicate hatası gibi göründüğü için yanlış yere bakılır). Tipik yer: bir dalda doldurulan, öbür dalda `X: null` diye "boş" bırakılan tarih/opsiyonel parametre. **Doğrusu:** değeri olmayan parametreyi nesneye HİÇ koyma (koşullu ekle: `if (v) { oP.X = v; }`); backend imzasında parametrenin opsiyonel olduğunu ve boş gelince doğru dalı işlediğini oku. Kontrol: (a) her `callFunction(... urlParameters: {...})` nesnesinde `: null` / `: undefined` ve koşula bağlı değer ara — **kardeş taraması iki app'in tüm function import'larını kapsar** (b) kanıt = kontrol grubu: aynı function import'u (tercihen yan etkisiz bir GET kardeşi) `=null` ile ve parametresiz çağır → 400 ↔ 200. ⚠ JSON payload'daki (create/update gövdesi) `null` bu sınıfa girmez — FE-04'e bak. | HIGH | ✅ grep: `urlParameters` bloğu içinde `: null` | S/4 private proje, freestyle V2 2026-09-23 (transfer function import'u; `Reconcile` GET kontrol grubu `=null` → 400, parametresiz → 200; `/IWFND/SU_ERRLOG` kaynağı `/IWCOR/CL_DS_URI`) |
| FE-48 | **Elle kurulan istek `sap-client` taşımıyor → iki client aynı tarayıcıda açıkken ÇAPRAZ-CLIENT okuma/yazma, hatasız** (2026-09-25). Manifest modeline Component `sap-client`/`sap-server` ekler; `new ODataModel(...)`, ham `fetch`/`XMLHttpRequest` ve `sServiceUrl +` ile kurulan URL bunu **almaz** — `sServiceUrl` UI5'te sorgusuz saklanır, parametreler `aUrlParams`'tadır (`ODataModel.js:396-405`). `sap-client`'sız istek tarayıcının TEK `sap-usercontext` çerezine göre yönlenir; çerezi en son açılan client'lı sayfa yazar ⇒ tek client açıkken görünmez, ikinci client açılınca ilk sekmenin elle kurulan istekleri öbür client'a gider (varyant listesi, yükleme/kaydet `$batch`, kilit bırakma). **Doğrusu:** ana (manifest) modelin `aUrlParams`'ını (`sap-statistics` hariç) URL sorgusuna devret — std/03 **§18.5b** `_secondModelUrl`; ana modeli util'de `Component.getOwnerComponentFor(ctrl).getModel()` ile al (onInit'te view modeli `undefined`). **Kontrol:** (a) `new ODataModel(` / `new XMLHttpRequest` / `fetch(` / `sServiceUrl +` her satırda URL'e ana modelin parametreleri ekleniyor mu — **kardeş taraması tüm app'leri kapsar** (tek util çoğu kez N kopyadır) (b) kanıt = iki client'lı iki sekme, **ayırıcı veri** (sayısı iki client'ta farklı entity) + ağ izinde `sap-client=<beklenen>`; kaynak okuması tek başına kanıt değil. FE-43'ün (`metadataUrlParams`) yerine geçmez, ikisi birlikte. | HIGH | (aday: `new ODataModel(`/ham istek → `aUrlParams` devri taraması; ADR 0019 — önce bu madde denenir) | S/4 private proje, freestyle V2 2026-09-25 (14 `TablePersonalizer` kopyası + 2 veri yazan `$batch` modeli + 5 kilit bırakma XHR'ı; 100/110 ayırıcı: varyant 3/0, lot 16/0) · prior-art: FE-43 (yalnız metadata ayağı) |
| FE-49 | **Sayfadan ayrılırken gönderilen istek SENKRON XHR → Chromium GÖNDERMEZ (navigasyonda ölçüldü); belge kilidi bırakılmaz** (2026-09-25). `beforeunload`/`pagehide`/`unload` içindeki `XMLHttpRequest(..., false)` Chromium'da sunucuya ulaşmaz, `try/catch` hatayı yutar ⇒ kilit ancak zaman aşımıyla düşer. Ölçülen (Chromium 153, lokal sunucu, üç olay × navigasyonla sayfadan ayrılış): senkron XHR **0/3** (normal anda 1/1 kontrol), `fetch(url,{method:"POST",keepalive:true,headers:{"x-csrf-token":…}})` **3/3** başlığıyla; gerçek fonksiyon gövdeleri (beforeunload, navigasyon) eski **0/5** · yeni **5/5**. Sekme KAPATMA ayırt edilemedi (`page.close({runBeforeUnload:true})`'da `sendBeacon` dahil hiçbiri ulaşmadı); Firefox/Safari/FLP ölçülmedi. `sendBeacon` özel başlık (CSRF) taşıyamaz. **Doğrusu:** `fetch` + `keepalive:true` + `x-csrf-token` (`oModel.getSecurityToken()` — kilit yalnız `AcquireLock` POST'undan sonra açık olduğundan token önbellekte) + URL'de ana modelin client parametreleri (FE-48) + sonuna `.catch`. **İkinci ayak:** bırakma artık GERÇEKTEN ulaştığı için, ekrandan çıkılan her yolda (geri/kayıt/silme) kilit bırakıldıktan sonra kilit bayrağı (`readOnly=true` ya da `_sId` temizliği) sıfırlanmalı — yoksa kullanıcı listedeyken sayfadan ayrılınca (ör. yenileme / başka adrese gitme) aynı belgeye tekrar bırakma gider ve kullanıcının **başka sekmede** tuttuğu kilidi düşürür (release `locked_by = sy-uname` siler). **Kontrol:** unload dinleyicisinde senkron XHR / `sendBeacon`+CSRF var mı; bırakma URL'i `/` + client parametresi taşıyor mu; `_releaseLock` çağıranlarından sonra bayrak sıfırlanıyor mu; unload dinleyicisi bayrağa bakıyor mu (`if (!readOnly && id)`). | HIGH | ✅ grep: repo genelinde `.open(` + `, false)` → çağrıyı unload dinleyicisine kadar izle (XHR çoğu kez dinleyicinin çağırdığı ayrı yardımcıdadır) | S/4 private proje, 5 app 2026-09-25 (2 app'te ayrıca eksik `/` → 307 → 403 `/IWFND/MED/170`) · howto-document-lock §4 · ADR 0014 değişiklik notu |
