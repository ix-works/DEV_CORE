# Bug-Checklist — Frontend (freestyle UI5 + OData V2)

> **Bug_Expert bunu kullanır** (ADR 0018). Her madde **must-do** — kozmetik buraya YAZILMAZ → **kanıtlı ihlal = ZORUNLU FİX** (builder takdiri değil). Kapsam = **diff + blast-radius**.
>
> **Automatable** sütunu G3 = `check_ui5_freestyle_traps.py` deterministik yakalar (LLM-yargısı değil) → Bug_Expert ÖNCE bunu koşar. Diğerleri semantik (kanıt-zorunlu inceleme).
>
> Kaynak: `../ui-freestyle-odata-v2.md` §K/§J · ADR 0008/0014/0017 · memory feedback'ler.

| ID | Kontrol (semptom) | Severity | Automatable | Ref |
|---|---|---|---|---|
| FE-01 | **V2 nav `_X`** (createEntry/$expand/path) — CDS composition adı; V2'de `to_X`. Semptom: Create kaydet sessiz boş / Change $expand boş | BLOCKER | **G3 T1** | §K2 |
| FE-02 | **Save = setProperty+submitChanges** (sıralı `oModel.update(merge)` değil). Semptom: programatik-set değer (auto-fill/cascade) kaydedilmiyor | HIGH | — | §K1 |
| FE-03 | **Eş-zamanlı `update`** aynı BO'da (sıralı zincir değil). Semptom: "Kaydedilemedi" (RAP BO kilit çakışması) | BLOCKER | — | §K1 |
| FE-04 | **MERGE'de boş tarih `""`** (null değil). Semptom: 400 Edm.DateTime | BLOCKER | — | §K1 |
| FE-05 | **`setData` eksik şekil** (`sel`/`hasSel`/`messages` düşmüş). Semptom: detay paneli seçmeden açık + seçince dolmaz | HIGH | — | §K3 |
| FE-06 | **`core:Title`** VBox/HBox/CSSGrid child (sap.m.Title değil). Semptom: view render crash "not valid for aggregation" | BLOCKER | **G3 T3** | §J |
| FE-07 | **`type=Number`** editable grid/miktar input (type=Text+liveChange değil). Filtre-sayaç istisna | WARNING | **G3 T2** | feedback_numeric-input-no-type-number |
| FE-08 | **Master-detail: load'da `removeSelections` yok**. Semptom: re-navigation'da bayat seçim → tık selectionChange'i yutar | HIGH | — | §K4 |
| FE-09 | **MERGE payload'da readonly `*Name`/`*Text`** (Edm hesaplanmış/assoc-path). Semptom: 400 / tüm MERGE reddedilir | HIGH | — | §K1 |
| FE-10 | **Plumbing sıfırdan yazılmış** (§K reuse yerine). Booking meta-dersi: çözülmüş mekaniği inferior desenle yeniden | HIGH | — | §K |
| FE-11 | **Referans edilen i18n key i18n'de tanımsız** — view `{i18n>k}` ve JS `_txt`/`getText("k")` literal'lerinin HER biri `i18n.properties` VE `i18n_tr.properties`'te tanımlı OLMALI. Pass/fail: kod-referans kümesi ⊆ tanım kümesi (iki dosyada da). Semptom: ham key ekranda (ör. "btn.cols") veya boş label. **Kör nokta:** kopyalanan util (TablePersonalizer'ın `_txt("var.*"/"btn.*"/"flt.*")` çağrıları) diff'te olmasa da kontrol edilmeli — çalışan app (booking) i18n'iyle çapraz-doğrula | WARNING | (aday: i18n-completeness validator) | §J · feedback_i18n-tr-her-iki-dosya · shipment "btn.cols" 2026-06-21 |
| FE-12 | **Recon kuralı dokümante, UI'a IMPLEMENTE değil** (gating/cascade). Semptom: kullanıcı "şu kural çalışmıyor" der | BLOCKER | — | feedback_done-tam-kapsam-dogrula |
| FE-13 | **Liste/rapor grid değil** (sap.ui.table + TablePersonalizer; **ADR 0008 = OTOMATİK-MUST**). m.Table YALNIZ mobil istisna | HIGH | `check_list_view_grid` (⚠️ ORPHAN — wire-aday) | feedback_grid-liste-standardi · ADR 0008 |
| FE-14 | **Audit alan auto-fill yok** (created/updated by-date-time, idempotent setAdmin) | HIGH | — | feedback_audit-alan-autofill-standardi |
| FE-15 | **Belge-kilidi / app-lock eksik veya yanlış** (read-only/heartbeat/beforeunload; ADR 0014) | HIGH | — | feedback_belge-kilidi |
| FE-16 | **Control API / nav-adı TAHMİN edilmiş** (canlı $metadata / ui5-mcp get_api_reference ile doğrulanmamış) | HIGH | — | §K2 / ADR 0017 |
| FE-17 | **"done" runtime-doğrulanmamış** (G1 smoke / en az G3 geçmemiş; node--check/XML-well-formed YETMEZ) | HIGH | — | ADR 0017 G4 |
| FE-18 | **EKSİK:** F4/VH alanında seçilen kodun **alt-tanımı (ad/açıklama) ekranda gösterilmiyor** (input + altında `<Text class="bookingFieldDesc">` deseni; başlık+detay her VH alanı). Kod çalışıyor → HATA değil, **standart eksik** | HIGH | — | feedback_belge-uygulama-ortak-ui-sablonu · §K |
| FE-19 | **EKSİK:** Ortak UI şablonu karşılanmamış (List filtre+akordion / başlık+kalem deseni; tüm belge-uygulamaları aynı layout) | MEDIUM | — | feedback_belge-uygulama-ortak-ui-sablonu |
| FE-20 | **Binding/MERGE-payload property adı canlı `$metadata`'da YOK** (eski alias / `ref_docs` CDS / **bayat `localService/mainService/metadata.xml`**'ten kopya → projeksiyon yeniden-adlandırılınca drift). FE-09'dan farkı: alan readonly değil, **hiç yok**. Semptom: başlık/diğer alanlar kaydolur ✅ / o alanı içeren kalem sessiz kaybolur veya "Kaydedilemedi" ❌ (SADL V2 deep-insert bilinmeyen property'yi SESSİZCE düşürür; MERGE'de 400). Kontrol: her save-payload + binding + picker-buffer property adını **aktif projeksiyon `$metadata`/adt_get** ile birebir doğrula — `localService/metadata.xml` ve `ref_docs` eski CDS bayat olabilir, GÜVENME | BLOCKER | — | ZSD001 booking `FinalAddress`→`FinalPartnerAddress` · sip_se `Charg`→`Batch` |
| FE-21 | **Generic hata yutma** — save/create/action error callback'i `that._t("msg.saveFail")` gibi sabit i18n gösterir, **gerçek `responseText` SAP mesajını parse ETMEZ**. Semptom: "Kaydedilemedi" çıkar ama kök-neden teşhis edilemez → kör iterasyon. Kontrol: her error callback `_parseError(oErr)` (responseText→`error.message.value`) ekleyip mesaja basmalı; createEntry/batch'te `__batchResponses`/`__changeResponses` ≥400 yanıtı da | HIGH | — | ZSD001 BaseOrder `_parseError` · feedback_done-tam-kapsam-dogrula |
| FE-22 | **Commit-barrier yok** — `onSave`/`onCreate` doğrudan `_save()` çağırır; `document.activeElement.blur()`→`setTimeout(_save,0)` sarmalayıcısı yok. Semptom: son düzenlenen Input/DatePicker JSON-model'e commit edilmeden gönderilir (değer kaybı / eski değer kaydolur) | HIGH | — | §1-B B7 · voyage Detail kanıtlı |
| FE-23 | **Paralel Create/Change controller'da fix-paritesi yok** (ortak base controller yerine kopya kod: save alan-listesi, cascade, VH map, clear-satırları). Semptom: bir bug birinde düzeltilir, ikiz controller'da kalır (`FinalAddress` İKİSİNDE de vardı). Kontrol: bir controller'da bulgu → ikiz controller'da aynı satırı doğrula | MEDIUM | — | ZSD001 BaseOrder paylaşılan-base dersi |
| FE-24 | **Kaydetme başarı GERİBİLDİRİMİ yok/kaybolur** — save success'te `MessageToast.show()` + **hemen** `navTo` → toast sayfa geçişinde KAYBOLUR (kullanıcı "mesaj gelmedi" der). Belge-no'suz generic "Kaydedildi" de zayıf. Kontrol: save success = **`MessageBox.success(belge-no'lu metin, { onClose: navTo })`** (modal, garantili görünür, navigasyon OK sonrası — ZSD001 stabil deseni). Her CRUD action'ında net geribildirim olmalı | HIGH | — | standards/03 §save-feedback · ZSD001 CreateOrder · voyage 2026-06-17 |
| FE-26 | **Manifest JSONModel `settings.data` ÇİFT-SARMALAMA** — manifest'te isimli JSONModel başlangıç verisi `settings` objesinin DOĞRUDAN içine yazılır (`"settings": { "busy": false, "filter": {} }`). `settings.data` ALTINA koyarsan (`"settings": { "data": {...} }`) UI5 tüm `settings` objesini constructor'a data olarak verir → veri `/data/...` altına düşer, **tüm `{model>/...}` kök-binding'leri (busy/filter/editable/locked vb.) SESSİZCE undefined** olur. Semptom: F4 seçimi yansımıyor (FE-25 görünümü), busy spinner hiç dönmüyor, filtre tutmuyor, read-only/lock state binding'i ölü. Kontrol: her isimli JSONModel için manifest `settings` içinde `data` anahtarı VAR MI + app `{model>/data/...}` kullanıyor mu? `/data/` referansı YOK ama `settings.data` VAR = bug (controller `setData/setProperty` ile maskelemiyorsa). Kanıt: `ui.getData()` runtime + `setProperty('/path')`→false | HIGH | (aday: manifest JSONModel `settings.data` taraması) | voyage F4 2026-06-18 (kök-neden) · sweep: 9 kardeş app |
| FE-27 | **OData V2 `Edm.DateTime`/`DateTimeOffset` alanına create/update payload'unda STRING geçme** → **JS Date objesi** olmalı (V2 model `/Date(epoch)/`'e serialize eder; düz string geçince backend deserialize edemez). Semptom: kaydet'te `Conversion error for property '<X>' at offset N`. **FE-04'ten farkı:** FE-04 = MERGE'de **boş** tarih `""`; FE-27 = **dolu ama string-tipli** (create/deep-insert dahil). Kök tuzak: `DatePicker value="{...}" valueFormat="yyyy-MM-dd"` modeli STRING tutar → payload'da `new Date(Date.UTC(y,m-1,d))`'e çevir (TZ off-by-one'a karşı UTC-parça), VEYA DatePicker'ı `dateValue=` (Date objesi) ile bağla. Kontrol: her save/create payload'undaki her Edm.DateTime property → Date objesi mi + **Create ↔ Change paritesi** (biri `dateValue` doğru, ikiz `value`-string kırık olabilir — FE-23 ile bağlı) | BLOCKER | (aday: DatePicker `value=`+`valueFormat`→payload-string taraması) | ZSD001 sip_se OrderDate 2026-06-19 (Create string→Conversion error; Change `dateValue` sağlamdı) |
| FE-28 | **Save/create/update payload property Edm-tipi CANLI $metadata ile EŞLEŞMİYOR** (genel kontrol; FE-27 = DateTime özel-hali). HER payload alanı canlı Edm-tipinde gönderilmeli: **Boolean** alan → `true/false` (DEĞİL `'X'/''` string), **Decimal** → packed→string direkt (`WRITE...TO` yok), **DateTime** → Date objesi. **KRİTİK: `localService/.../metadata.xml` mock BAYAT olabilir (FE-20) → CANLI `$metadata`/adt_get YETKİLİ, mock'a GÜVENME.** READ-tarafı da: Boolean alanı `=== 'X'` varsayan formatter/binding (ObjectStatus/Switch/CheckBox) boolean'a göre olmalı. Semptom: kaydet'te `Failed to read property '<X>' at offset N` (Boolean'a string) veya `Conversion error` (DateTime). Kontrol: bug-expert her Create+Change save-payload property'sini **canlı $metadata Edm-tipiyle birebir** eşler (alan-alan tablo); mock-kopya kabul edilmez. Create↔Change paritesi (FE-23) | BLOCKER | (aday: payload-property → canlı-$metadata Edm-tip diff validator) | ZSD001 sip_se Blocked 2026-06-19 (Edm.Boolean'a `'X'` → Failed to read; mock String diyordu) |
| FE-25 | **VH/F4 seçimi alana DEVRALINMIYOR** (dialog açılır+liste gelir, ama seçilen kayıt input'a/modele yazılmaz). FE-18'den farkı: FE-18 = ad GÖSTERİLMİYOR (kod çalışıyor); FE-25 = değer hiç YAZILMIYOR (kod yanlış). **Statik 3-nokta write-back izi** (her `valueHelpRequest=".onF4X"` Input için): (1) Input value-hedefi = `{model>/path}` + binding-context, (2) `.onF4X`→SelectDialog `confirm`→`setProperty(...)` üçlüsü, (3) **birebir eşleşmeli**. İhlal: yanlış model (Input "v"/default ↔ confirm "ui"), yanlış path, context uyumsuz (mutlak path ama Input satır-aggregation'ında / tersi), confirm `selectedItem` okumuyor/hiç yazmıyor, **yalnız `oInput.setValue()`'a güvenip** modele yazmamak (OneWay refresh siler), ya da item template `title`/`description` VH entity'sinde olmayan property'ye bağlı (`getTitle()` boş). Çalışan referansla (örn. Detail `_openVH`) kıyasla — fark = bug | HIGH | — | feedback_belge-uygulama-ortak-ui-sablonu · voyage filtre F4 2026-06-18 |
| FE-29 | **Save akışı callback-garantisiz → busy SONSUZ döner / kaydet bitmez** — `onSave` kendi `aUpdates` dizisi + custom `fnNext` özyinelemesiyle `oModel.update(merge)` çağrılarını **eager `createEntry`/`remove` + sonda tek `submitChanges`** ile KARIŞTIRIR (kanıtlı `_runSeq` sıralı-runner yerine), ve/veya busy'yi `getView().setBusy()` ile callback içinde aç/kapatır. Semptom: Kaydet'e basınca **busy spinner sonsuz döner, ne success ne error** — bir callback yolu hiç ateşlenmez / pending-changes flush edilmez / busy açık kalır. FE-02 (programatik-değer kaybı) ve FE-03 (kilit çakışması)'nın **somut "hiç bitmeyen save" hali**; ayrıca FE-10 (plumbing sıfırdan) meta-dersinin doğrudan sonucu. **Doğru desen (sip_se/ihr_se kanıtlı §K):** `ui>/busy` flag → header MERGE entity-path (`oModel.createKey`) `{merge:true,success,error}` → success içinde child persist-diff'i **`_runSeq(aSaves, doneCb, errCb)`** ile SIRALI (her adım `function(done,fail){...success:done,error:fail}` = GARANTİLİ tamamlanır) → child GÜNCELLE/SİL **entity-path** (`createKey`, KEY gövdede değil), child EKLE **nav-path** (`oModel.create(sPath+"/to_X")`) → kısmi-fail'de `_reload()` resync. Kontrol: save akışı kanonik `_runSeq` desenini mi kullanıyor, yoksa ad-hoc `update`-loop+`submitChanges` karışımı mı? Her olası yol (0 kalem / sadece-update / sadece-create / karışık) success VEYA error'a ULAŞIYOR mu? | BLOCKER | — | §K1 · sip_se ChangeSe `_runSeq` · ZSD001 booking 2026-06-20 |
| FE-30 | **Client-only `_`-property OData V2 entity context'e `setProperty` ile stamp** (havuz-merge sonucu, ör. `_miktarPak`/`_custMaterial`) → entity `mChangedEntities`'e pending-change girer + `hasPendingChanges()` true döner. Pass/fail: read-only liste için GÜVENLİ (app `submitChanges`/`createEntry` kullanmıyorsa) AMA aynı OData model bunları kullanıyorsa bilinmeyen `_x` property backend'e sızar → deep-insert/MERGE 400. Kontrol: OData context'e `_`-prefix `setProperty` var mı + aynı model `submitChanges`/`createEntry` çağırıyor mu? Varsa client-only stamp **parallel JSONModel'e (path-keyed)** yazılmalı, OData context'e DEĞİL | MEDIUM | (aday: OData-context `_`-property setProperty + submitChanges taraması) | ZSD001 shipment ③ `_miktarPak`/`_custMaterial` 2026-06-21 (bg-pak latent) |
| FE-31 | **VH/F4 dialog açılır ama liste BOŞ — VH entity-set'i raporun SRVD servisinde EXPOSE EDİLMEMİŞ** (canlı `$metadata`'da EntitySet yok). FE-25'ten farkı: FE-25 = liste GELİR ama seçim yazılmaz (FE kodu yanlış); FE-31 = liste HİÇ GELMEZ çünkü `SelectDialog items:{path:"/ZSD000_I_X_VH"}` bind ettiği entity-set servis metadata'sında YOK → binding sessiz boş. FE-20'den farkı: FE-20 = save-payload property yok; FE-31 = tüm VH entity-set'i yok. **Kök = BACKEND config** (FE doğru olabilir): generic VH rename/sweep sonrası bir tüketici raporun SRVD'sine `expose` eklenmesi atlanır. Kontrol: FE'nin bind ettiği HER VH entity-set'i (`_openFilterVH`/`_openVH`/cfg `set:`) → raporun SRVD'sinde `expose <VH>;` VAR MI (adt_get srvd) + canlı `/sap/opu/odata/sap/<SRVB>/$metadata`'da EntitySet olarak görünüyor mu. Generic VH (ZSD000_I_*) değişince TÜM tüketici servisleri tara (tek-yer-düzelt eksik; [[feedback_fix-oncesi-where-used-blast-radius]]). Düzeltme additive: SRVD'ye expose ekle + aktive + SRVB republish (FE/redeploy gerekmez) | HIGH | (aday: FE VH `set:` → tüketici SRVD expose + canlı $metadata EntitySet diff) | ZSD001 container_report ShiptoPartner/ShippingPartner (CUSTOMER_VH/VENDOR_VH expose eksik) 2026-06-22; kök = 2026-06-17 BPNAME→CUSTOMER/VENDOR sweep |

| FE-32 | **Rapor/liste filtre+VH+grid arama deseni** (SELECT-OPTIONS standardı, 2026-06-24). **(a) HATA·BLOCKER:** filtre/VH/grid arama Filter'ında `caseSensitive:false` KULLANMA → UI5 V2 `$filter`'da `toupper()`/`tolower()` üretir, SAP Gateway (/IWBEP) bunları DESTEKLEMEZ → **HTTP 400 "Function toupper/tolower is not supported"** (SAP Note 1797736) → arama hiç sonuç döndürmez. Düz `new Filter(path, FilterOperator.Contains, q)` (caseSensitive parametresi VERME) → düz `substringof` zaten HARF-DUYARSIZ (DB collation; canlı: "gül"→"GÜLAK" 200). **(b) EKSİK·WARNING:** rapor filtre ekranı tek-değer `<Input>` değil **`<MultiInput>` + ValueHelpDialog (çoklu-değer + aralık)** olmalı; **(c)** wildcard `_parseSearchTerm` (`*x*`/`x`→Contains, `x*`→StartsWith, `*x`→EndsWith; startswith/endswith /IWBEP'te DESTEKLENİR, toupper DEĞİL); **(d)** serbest-metin/kod alanlarında düz-token default `Contains` (`defaultOp`); **(e)** grid sütun-başlığı filtresi (`TablePersonalizer._onColumnFilter`) string kolonda düz Contains (caseSensitive YOK) + wildcard. **Kanonik referans: ZSD001 sales_order_report.** | BLOCKER | `check_filter_search_pattern.py` | standards/03 §filtre-arama · playbook/ui-freestyle-odata-v2.md · ZSD001 2026-06-24 (caseSensitive:false→/IWBEP 400) |
| FE-34 | **F4/value-help alanında ELLE girilen değer VH'de yoksa doğrulanmıyor + kayıt bloklanmıyor** (İŞ KURALI, 2026-07-02). VH ile doldurulabilen bir alana kullanıcı elle/yapıştırma değer girebilir; girilen değer VH'nin geçerli değerleri arasında YOKSA: **(a)** Input `setValueState("Error")` + açıklayıcı `valueStateText`; **(b)** create/save **BLOKLANMALI** (geçerli değer seçilene/girilene veya alan boşaltılana kadar). Geçerli olma yolları (VH'de VAR / F4 seçimi / boş / başarılı-create) → `valueState("None")` + invalid-flag temizlenir. Amaç: geçersiz kod/referansla veri bütünlüğü ihlali önlenir — backend reddine bel bağlamadan kullanıcıya anında geri bildirim. **Güvenilir save-guard = veri-modeli flag'i** (`<X>Invalid`), yalnız valueState'e güvenme (çok-satır/context geçişinde kaybolur). Kontrol: her VH-bağlı Input (`valueHelpRequest`/`showValueHelp`) için change-handler değeri VH-entity'sine karşı doğruluyor mu + save/create handler geçersiz-flag'i denetleyip bloklyor mu. Kanıt: geçersiz elle-giriş → valueState Error + create/save çağrısı YAPILMIYOR (Playwright). | BLOCKER | (aday: VH-input change-validation + save-guard varlık taraması) | standards/03 §f4-manuel-giris-validasyonu · ZSD001 şoför Kimlik No 2026-07-02 (geçersiz elle-giriş kayda izin veriyordu) |
| FE-33 | **`sap.ui.layout.form.Form`/`SimpleForm` + `ColumnLayout`(veya ResponsiveGridLayout) içinde `f:fields`/form-content'e HBox/VBox/FlexBox/Panel gibi LAYOUT-CONTAINER konma** — form-content yalnız geçerli form kontrolü kabul eder (Input/Button/Text/ComboBox/CheckBox...); container = GEÇERSİZ. Semptom: **diyalog/view HİÇ AÇILMAZ** (kullanıcı "buton çalışmıyor" der), konsol: *"Element sap.m.HBox is not a valid Form content! Only use valid content in ...ColumnLayout"*. **KÖR NOKTA (bu bug tam buradan kaçtı):** aynı HBox-deseni **CSSGrid/VBox**'ta GEÇERLİ (ör. booking) → başka app'ten kopyalanan HBox+FlexItemData düzenini Form/ColumnLayout'a taşımak crash yapar. Layout-container tipini alan-yerleşimine göre doğrula. Çoklu-kontrolü tek hücrede: HBox yerine **doğrudan sibling `f:fields`** (Input+Button+Text) veya **ayrı `f:FormElement`** satırı. **Kontrol:** her `f:Form`/`SimpleForm` içindeki `f:fields`/content çocuğu container mı (HBox/VBox/FlexBox/Panel) → BLOCKER. **node--check/XML-well-formed BUNU YAKALAMAZ** (geçerli XML ama geçersiz UI5 aggregation → yalnız runtime render'da patlar) → **runtime açılış doğrulaması ZORUNLU (FE-17: diyaloğu AÇ, konsol temiz mi).** | BLOCKER | (aday: G3 — `f:Form`/ColumnLayout içi HBox/VBox/FlexBox içerik taraması) | ZSD001 shipment NewBooking/ContainerAdd 2026-07-02 (HBox in ColumnLayout → diyalog açılmadı; bug-gate kaçırdı çünkü XML-valid + render-doğrulanmadı) |
| FE-35 | **Miktar/quantity alanı HAM binding — birim-ondalık formatter'sız → OData QUAN "14.000" gösterir ("14" yerine)** (2026-07-05). OData V2 QUAN property'si (ADT/ADET gibi 0-ondalıklı birim dahil) FE'ye ham `abap.dec(x,3)` string olarak gelir; `<Text text="{se>/activeItem/DeliveredQty} {se>/activeItem/SalesUnit}"/>` gibi DÜZ-CONCAT binding trailing-zero'ları OLDUĞU GİBİ basar → "14.000 ADT". **Kör nokta:** aynı app'in GRID/tablo hücreleri formatter kullanır (doğru) ama sağdaki KALEM-DETAY paneli ham-concat kalır (kopyalanırken atlanır) → tablo "14", detay "14.000". Kontrol: her miktar `<Text>`/`<Label>` binding'i (Qty/Miktar/Bakiye/Remaining/Delivered) formatter kullanıyor mu (`.formatter.qtyPak` veya eşdeğeri; `Math.round(q*1000)/1000` + `String()` trailing-zero kırar) yoksa düz-concat mı. Düz-concat = EKSİK. Düzeltme: `{ parts:['...Qty','...SalesUnit'], formatter:'.formatter.qtyPak' }` (PAK istenmiyorsa vFactor GEÇME). Kanonik: ihr_se ChangeSe detay. | MEDIUM | (aday: G-qty — miktar-binding formatter'sız düz-concat taraması) | ZSD001 fih/dsk/fittings_se kalem-detay 2026-07-05 ("14.000 ADT"; tablo doğru, detay ham) |

> **TİP:** Her madde **HATA** (kod yanlış) veya **EKSİK** (kod çalışıyor ama must-do/UX standardı karşılanmamış) tipindedir → Bug_Expert raporda `[HATA]`/`[EKSİK]` etiketler, ikisi de **zorunlu** (pass geçilmez). Çoğu FE-01..09 + FE-20/22/25/26/27/28/29/31/33/34 = HATA (FE-06/FE-33 = view/diyalog render-crash; FE-34 = geçersiz-giriş save-guard); FE-13/14/15/18/19/21/23 = EKSİK (UX/completeness/diagnosability). Checklist-DIŞI fikir = `[ÖNERİ]` (bağlayıcı değil).
> **Pre-existing AYRI:** değişimle ilgisiz görülen kritik = ayrı işaretle, bu değişimi bloklama.
