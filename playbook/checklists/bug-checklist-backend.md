---
applies_to: [s4_private]
---
# Bug-Checklist — Backend (ABAP / RAP / CDS / DDIC)

> **Bug_Expert bunu kullanır** (ADR 0018). Her madde **must-do** — **kanıtlı ihlal = ZORUNLU FİX**. Kapsam = **diff + blast-radius** (where-used: çağıran CDS/class/BDEF; released-ref; tablo alan).
>
> **Automatable** sütunu: `adt_syntax_check` / `adt_atc_check` / mevcut validator deterministik yakalar → Bug_Expert ÖNCE bunları koşar. Diğerleri semantik.
>
> Kaynak: `../adt-rap.md` §32/§35 · `../../standards/05-coding-rap.md` · `../checklists/*-creation.md` · memory feedback'ler · ADR 0005/0006/0009.

| ID | Kontrol (semptom) | Severity | Automatable | Ref |
|---|---|---|---|---|
| BE-01 | **Standart obje/tablo direkt yazım** (Z olmayan create/değiştir/sil; standart tabloya INSERT/UPDATE) | BLOCKER | (reviewer/guardrail) | ADR 0005 A/B |
| BE-02 | **DTEL/append/domain adı TAHMİN/ÖNERİ** (kullanıcı/lider vermeden) | BLOCKER | — | ADR 0005-A · feedback_zli-obje-text-tahmin |
| BE-03 | **Standart tablo kullanımı, released-CDS yerine** (MARA vs I_Product; released_successors) | HIGH | check_released_objects | feedback_clean-core-released-cds |
| BE-04 | **decimal/qty `WRITE...TO`** API body'ye (binlik ayıraç → Edm.Decimal 400). Packed→string direkt | BLOCKER | check_decimal_write_to.py | feedback_abap-decimal-odata-serialize |
| BE-05 | **Namespaced DTEL DDL'de tek-tırnak** (`/scwm/..` tırnaksız küçük-harf olmalı; tek-tırnak sessiz düşer → "active differs") | HIGH | — | feedback_namespaced-dtel-ddl-tirnaksiz |
| BE-06 | **BDEF mapping eksik** (alan map yok → I/C aktivasyonu "mapped field yok" kırar) | HIGH | adt_activate | feedback_inline-post-empty-source |
| BE-07 | **inline-POST boş source** (CDS children boş; create sonrası SOURCE içeriği doğrulanmamış) | HIGH | adt_get readback | feedback_inline-post-empty-source |
| BE-08 | **`\_Text` persist edilmemiş** (unmanaged static-action; buffer-başarı ≠ persist → `SAVE_TEXT savemode_direct`) | HIGH | — | adt-rap §35/§36 |
| BE-09 | **editableFieldFor key set edilmemiş** (Released BO CREATE BY _assoc; `<Key>ForEdit`, key değil) | HIGH | — | feedback_rap-editablefieldfor-key-create |
| BE-10a | **Source-based class method-imza tip-tuzağı** (`TYPE c LENGTH n`/CURR DTEL/`RANGE OF ZZ1_*` method imzasında save-scan kırar → `TYPE string`/named-DTEL) | HIGH | `check_method_param_type_c` (WIRED) + adt_syntax_check | feedback_source-based-class-type-c-trap · adt-classes §24.6 |
| BE-10b | **Yetim (detached) abapdoc** (metot/deklarasyon-arası, koda-bağsız yorum → save 400 "unknown comments") | HIGH | gate YOK → adt_syntax_check belirtisi (post-hoc) | feedback_detached-leading-abapdoc |
| BE-11 | **Audit alan auto-fill yok** (created/updated; idempotent setAdmin det, instance-guard cyclical-dump) | HIGH | check_audit_fields_autofill | feedback_audit-alan-autofill |
| BE-12 | **ATC Priority-1 düzeltilmemiş** (Prio 2/3 ancak açık onayla pass; variant ZZNDBS_ATC). **En sık Prio-1 = LOOP-içi-SELECT (Nested Reading DB OP):** `LOOP AT..ENDLOOP` VEYA `FOR..IN`/tablo-comprehension İÇİNDE `SELECT`/`SELECT SINGLE` → prefetch'e çevrilmeli (loop-öncesi toplu okuma + bellekte `READ TABLE`; BE-44 tuzağına dikkat). standards/04 zaten "Loop içi SELECT yasak". **⚠ KRİTİK — SADECE ATC'ye GÜVENME:** `adt_atc_check` yalnız class SAP'de DERLENİNCE koşar; class save/aktivasyon-bug'lıysa ATC **BLOKE** → bug-expert "ATC koşamadı" deyip PASS verir, LOOP-SELECT en sona kalır (ZSD001 EXCUPL 2026-07-12: 3 save-bug ATC'yi geciktirdi → 5 Prio-1 loop-SELECT ancak class derlenince çıktı, build-sonrası pahalı refactor). **ÇÖZÜM: LOOP-içi-SELECT'i STATİK oku/grep ile de flag'le** — `LOOP AT`/`FOR .. IN` ile `ENDLOOP`/`)` arası `SELECT` var mı; ATC koşamıyorsa statik tespit ZORUNLU. | HIGH | adt_atc_check (compile SONRASI) **+ STATİK** grep `SELECT` in `LOOP AT..ENDLOOP`/comprehension (compile ÖNCESİ, ATC bloke ise ZORUNLU) | feedback_atc-priority-1-zorunlu · standards/04 · ZSD001 EXCUPL 2026-07-12 |
| BE-13 | **Tablo onay gate atlandı** (alan+key+DTEL tasarımı gösterilip açık onay alınmadan create) | BLOCKER | — | feedback_tablo-yaratma-onay-gate |
| BE-14 | **API call iç-gateway proxy yok** (RFC-dest/SM59 → `ZBC001_CL_GET_TOKEN` + iç loopback; host/client runtime) | MEDIUM | — | feedback_api-call-ic-gateway-proxy |
| BE-15 | **Write sonrası readback yok** (adt_get active ile beklenen alan/içerik doğrulanmamış; "uploaded/activated" yanıltıcı) | HIGH | **WIRED: readback-gate** (atom.py `_content_readback` activate sonrası source-based normalize-compare → mismatch=ok:False; `_exists_after_delete` delete sonrası varlık) + adt_get (XML-DDIC alan-verify) | feedback_push-failure-stale-lock · ZSD001 `where`-kaybı 2026-06-21 |
| BE-16 | **Tablo alan DROP / TYPE/RENAME** (where-used + struct/view/CDS impact + bakım-view re-gen doğrulanmamış) | BLOCKER | check_table_field_drop | feedback_reviewer-checklist-vs-wired |
| BE-17 | **Released BO action `editableFieldFor`/scope kaçışı** (projeksiyon CDS source okunmadan BAPI/scope'a kaçma) | MEDIUM | — | adt-rap §35 |
| BE-18 | **Recon kuralı dokümante, BE'ye IMPLEMENTE değil** (gating/determination/validation) | BLOCKER | — | feedback_done-tam-kapsam-dogrula |
| BE-19 | **Generic BP VH/ad-çözümü** (partner alanı için `ZSD000_I_BPNAME`/but000/BU_PARTNER join/assoc — değer DAİMA customer/KUNNR→`I_Customer` ya da vendor/LIFNR→`I_Supplier`; BU_PARTNER≠KUNNR/LIFNR → boş/yanlış ad) | HIGH | grep BPNAME/BusinessPartner | standards/05 §9B · sweep 2026-06-17 |
| BE-20 | **`READ ENTITIES BY \_assoc FROM <key>` ile NON-KEY alan okuma** — `BY \_assoc FROM` YALNIZ key alanları döner; non-key (tarih/durum/tutar/tip) INITIAL kalır → validation/determination SESSİZCE yanlış çalışır (syntax 0-error, ATC temiz, aktive olur → **yalnız RUNTIME'da çıkar**). Non-key okuyacaksa **`ALL FIELDS WITH`** (veya `FIELDS ( ... ) WITH`, `FROM` değil) ŞART. Semptom: existence/key kontrolü geçer ama alan-değeri kontrolü hep fire eder / hiç fire etmez (strateji-skip). ZSD001 doğru (`ALL FIELDS WITH`) | HIGH | check_rap_byassoc_keys_only | standards/05 §5.1 · feedback_rap-by-assoc-read-all-fields · voyage/sevkemri/booking 2026-06-17 |
| BE-21 | **RAP result-entity satırı `%param`-nested DEĞİL** — `RESULT [..] <abstract entity>` (result parameter) döndüren action'da sonuç satırının entity alanları **`%param` ALTINA** yazılmalı (`%cid` dışında); flat `APPEND VALUE #( %cid = .. Success = .. ) TO result` → aktivasyonda `No component exists with the name "<FIELD>"`. `RESULT [..] $self` ise `%tky`+`%param=<entity>`. **run_review/abaplint YAKALAMAZ → yalnız bactivate'te çıkar.** Kanıt desen: ZSD001_BOOKING.ccimp `%param = s` | HIGH | gate YOK → adt_syntax_check/bactivate (post-hoc) | ZSD001 R6 2026-06-20 |
| BE-22 | **Generic tablo param + `LOOP..WHERE`/component erişimi** — metot param `TYPE STANDARD/INDEX/ANY TABLE` (satır-tipi generic) iken `LOOP AT it ASSIGNING <x> WHERE comp = ..` / `READ TABLE WITH KEY comp=` → aktivasyon `row type must be identifiable statically in WHERE`. Somut `TYPES tt_x TYPE STANDARD TABLE OF <struct>` ile tiple. **abaplint YAKALAMAZ.** | HIGH | gate YOK → adt_syntax_check/bactivate | ZSD001 R6 2026-06-20 |
| BE-23 | **Typed-param key-uyumsuzluğu** — çağıranın yerel tablosu (`TYPE STANDARD TABLE OF x` = default key) typed-param'a (`tt_x ... WITH EMPTY KEY`) geçince `not type-compatible with formal parameter`. Param somut tiplenince ÇAĞIRAN decl'i de aynı typed-alias'a (`TYPE tt_x`) çevir. BE-22'nin türevi; aynı turda tara. | MEDIUM | gate YOK → adt_syntax_check/bactivate | ZSD001 R6 2026-06-20 |
| BE-24 | **Released BO EML operasyonu sistemde KAPALI** (`The operation "UPDATE/CREATE" is not activated for entity "<I_...TP>"`) — released BO'ya `MODIFY ENTITIES OF <released_tp> ... UPDATE` yazılmadan ÖNCE o BO'nun ilgili operasyona açık olduğu canlı doğrulanmalı; kapalıysa EML değil **OData V2 MERGE/PATCH** (iç gateway proxy ZBC001; CFL alanları servise expose+publish) ya da released değişiklik-BAPI. **Aktivasyona kadar görünmez** (EML çözümlemesi en sonda). | HIGH | gate YOK → bactivate | ZSD001 R6 2026-06-20 · BE-14 |
| BE-25 | **Released-CDS SELECT alan adı uydurma/yanlış** — released view'den `SELECT`/component (ör. `OverallPickStatus` ≠ canlı `OverallPickingStatus`) → aktivasyon `Unknown column name` / `does not have a component`. Released CDS alan adlarını **canlı view source (adt_get)** ile doğrula, hatırdan yazma (clean-core field-verify; <LEGACY_SOURCE>-field-system-dependent dersi). | HIGH | gate YOK → adt_syntax_check | ZSD001 R6 2026-06-20 · feedback_<legacy_source>-field-adlari-sistem-bagimli |
| BE-26 | **RAP handler/helper class'ta DB-transaction kontrolü** — RAP managed/unmanaged behavior pool (ccimp) VEYA action handler'dan çağrılan helper class içinde `COMMIT WORK`/`ROLLBACK WORK`/`BAPI_TRANSACTION_COMMIT`/`BAPI_TRANSACTION_ROLLBACK`/`COMMIT ENTITIES` → runtime **`BEHAVIOR_ILLEGAL_STATEMENT`** dump (SAPLBAPT), bağlantı reset, "HTTP request failed" (dump mesajı yutar). **Static-check/syntax/ATC/abaplint GEÇER → yalnız ilk gerçek RUNTIME testinde çıkar** (kanıt: ZSD001 create_transport_doc, 3 bug-gate+syntax+ATC atladı). Commit gerektiren klasik BAPI (BAPI_SHIPMENT_CREATE, SD_SCDS_CREATE/`i_opt_commit='X'`, vb.) → **ayrı LUW**: Z RFC-enabled FM + `CALL FUNCTION '...' DESTINATION 'NONE'` (commit yalnız o FM'de legal). FM-içi-commit (i_opt_commit) DİREKT çağrı da aynı tuzak. Gerçek non-RAP class istisnası: `"#NO_RAP_COMMIT_CHECK <gerekçe>`. | BLOCKER | check_no_rap_commit | ZSD001 nakliye create 2026-06-23 · playbook/adt-rap.md · feedback_kural-gate-lenmeli (ADR 0019) |

| BE-28 | **AMDP CDS table function — ilk-kez gotcha seti (aktivasyon-only, SQLScript body)** — yeni AMDP table function yazarken bu 4'ü ÖNCEDEN tara, yoksa aktivasyon tek-tek ısırır (her round 1 co-activation = pahalı): **(a) client-dependent set** = `@ClientHandling.type:#CLIENT_DEPENDENT` (header) + `with parameters @Environment.systemField:#CLIENT clnt:abap.clnt` (PARAMETRE, return-field DEĞİL) + Mandt return ilk eleman annotationsuz + AMDP method `clnt` param `WHERE mandt=:clnt` (SDDL_PARSER_MSG 156/157). **(b) body PUR ASCII-7** — `--` yorumlarında bile Türkçe/sembol (ş/ı/⨝/Σ/→/—) YASAK ("Only ASCII 7 bit"). **(c) `--` yorumda APOSTROF YASAK** — Türkçe ek (`knumv'de`/`voyage'a`) + tırnak-içi-değer anımı (`'C'`/`'EUR'`) "literals span more than one line" verir; her schema-edit yeni yorum getirdiğinde TEKRARLAR (4×; son: ZSD001 voyage-decouple 2026-06-29 — bug-gate PASS dedi, activate patladı). **→ WIRED: `check_amdp_comment_apostrophe.py` (BE-28c, run_all + run_review class_push, BLOCKER)** — artık bug-expert'in hatırlamasına bağlı değil, deterministik. **(d) namespaced tablo** (`/ITTR/...`) AMDP USING↔FROM'da HANA-side erişilemez (AMDP_HDB 002) → Z-view arkası veya alan sabit-boş fallback. Kaynak-kolon adları + join key'leri (<LEGACY_SOURCE>≠S/4, ör. VFSI link=knumv kposn, fknum/fkpos DEĞİL) canlı adt_table_read ile teyit (feedback_<legacy_source>-field-adlari-sistem-bagimli). | HIGH | gate YOK → co-activate/adt_syntax_check + edit-sonrası apostrof/ASCII grep-guard | ZSD001 SATNAV AMDP 2026-06-24 · playbook/adt-cds (AMDP) |
| BE-27 | **CDS capability iddiası versiyon-çıkarımıyla DEĞİL, canlı-kanıtla doğrulanır** — bir CDS fonksiyonu/özelliğinin (ör. `string_agg`, yeni built-in, agregat) "bu sürümde destekli" iddiası S4CORE/sürüm-numarasına bakılarak VARSAYILMAZ. Codebase'de **proven-precedent** (canlı-aktif kullanan başka view) yoksa şüpheci ol; aktivasyon kanıtı olana kadar fallback hazır tut. Somut tuzak: `string_agg(col,', ')` ABAP CDS view-entity'de YOK → aktivasyon `Column <col> is not contained in GROUP BY list` → 1:N liste için native `count`+`max`-temsilci (playbook/adt-cds T9). **Reviewer "S4CORE=NNN → destekli" çıkarımını KABUL ETME** — proven-precedent veya bactivate iste. | HIGH | gate YOK → bactivate/adt_syntax_check + proven-precedent grep | ZSD001 SEREP string_agg 2026-06-24 · playbook/adt-cds T9 |
| BE-29 | **`COLLECT` hedef tablosunda non-key alan NUMERİK değil** — `COLLECT wa INTO itab` ancak itab'ın TÜM non-key bileşenleri numerik ise derlenir; CHAR/non-numeric bir non-key alan (vrkme/name1/birim/cuky) varsa aktivasyon `COLLECT command can only be used if all non-key components are numeric`. Çözüm: o alanı UNIQUE KEY'e al **veya** COLLECT yerine `READ TABLE..KEY` + manuel toplama (granülarite korunarak — key'e alma satır/müşteri bölünmesin diye). **run_review/abaplint GEÇER → yalnız aktivasyon/adt_syntax_check'te çıkar.** **Statik de flag'le (BE-12 dersi — ATC/syntax bloke olabilir):** grep `COLLECT` → hedef tabloda CHAR/non-numeric non-key alan var mı, kodu okuyarak teyit. | HIGH | gate YOK → adt_syntax_check + **STATİK** grep `COLLECT` (compile öncesi) | ZSD001 TERMIN_RAP 2026-06-25 |
| BE-30 | **Yabancı/standart metot param IMPORTING↔CHANGING karışması** — `cl_gui_alv_grid=>set_table_for_first_display`'de `it_outtab`/`it_fieldcatalog`/**`it_sort`** **CHANGING** parametreleridir; EXPORTING'e koymak → aktivasyon `formal parameter "IT_SORT" is a CHANGING parameter, not IMPORTING`. Genel kural: yabancı metot çağrısında param-yönünü İMZADAN teyit et (özellikle ALV grid first_display). **abaplint/run_review GEÇER → yalnız aktivasyonda çıkar.** **Statik de flag'le (BE-12 dersi):** yabancı-metot çağrısında (özellikle `set_table_for_first_display`) param yönünü İMZADAN teyit et — kodu okuyarak, ATC/syntax'ı bekleme. | MEDIUM | gate YOK → adt_syntax_check + **STATİK** imza-teyit (compile öncesi) | ZSD001 TERMIN_RAP 2026-06-25 |
| BE-31 | **Ham standart-tablo SELECT → ATC Priority-1 (released successor ŞART)** — bazı standart tablolar (özellikle **KNA1** BP modeli; LFA1 vb.) ham `SELECT` ATC variant'ında **Prio-1** verir → released CDS'e çevrilmeli (KNA1→`I_Customer`, LFA1→`I_Supplier`). Salt-okuma raporda bile zorunlu (Prio-1 mandatory, feedback_atc-priority-1-zorunlu). Bug-expert ham KNA1/LFA1 SELECT gördüğünde flag; **syntax temiz, yalnız ATC'de çıkar.** | HIGH | gate YOK → adt_atc_check (Prio-1) | ZSD001 TERMIN_RAP 2026-06-25 · feedback_clean-core-released-cds-proaktif |
| BE-32 | **CDS aggregate (MAX/MIN/SUM) UNIT/CUKY-tipli kolon üzerinde** — `max( vrkme )`/`max( waers )` gibi UNIT (ölçü birimi) veya CUKY (para birimi) tipli alanı aggregate fonksiyona sokmak → aktivasyon `Function MAX: Type UNIT/CUKY of Parameter 1 not supported`. Çözüm: alanı aggregate ETME, **GROUP BY listesine al** (kalem-seviyesinde fonksiyonel-sabit → kardinalite nötr). UNION'da iki dalı da aynı şekilde düzelt (tip-uyumu). **run_review/abaplint GEÇER, bug-expert DDL'den statik görebilir (max(<unit/cuky-field>) ara) → yalnız aktivasyonda çıkar.** Proven-precedent: ZSD001_I_FREIGHT_COST (CUKY), ZSD001_I_DELIVERY_QTY (UNIT GROUP BY). | HIGH | gate YOK → adt_syntax_check + DDL grep `(max\|min\|sum)\( *\w*(vrkme\|waers\|meins\|...)` | ZSD001 SCHED_HIST/ALL 2026-06-25 · ZSD001_I_FREIGHT_COST |
| BE-33 | **UNION view-entity header'da `@Metadata.ignorePropagatedAnnotations: true` eksik** — `define view entity ... union [all] ...` propagate-edilebilir element-annotation taşıyorsa SAP header'da bu annotation'ı ZORUNLU kılar → aktivasyon `Annotation Metadata.ignorePropagatedAnnotations is required`. Çözüm: header'a `@Metadata.ignorePropagatedAnnotations: true` ekle. **run_review/abaplint GEÇER, bug-expert DDL'den `union` görüp header'da annotation yoksa flag edebilir → yalnız aktivasyonda çıkar.** Proven-precedent: ZSD001_I_ORDER_ITEMS, ZSD001_I_BATCH_STOCK. | MEDIUM | gate YOK → adt_syntax_check + DDL grep (`union` var + `ignorePropagatedAnnotations` yok) | ZSD001 SCHED_ALL 2026-06-25 · ZSD001_I_ORDER_ITEMS |
| BE-34 | **S/4 satış belgesi key-user alanları (ZZ1_*_SDH) BAPI EXTENSIONIN'de `BAPE_VBAK`'ta DEĞİL → `BAPE_SDSALESDOC`'tadır.** BAPI_SALESDOCU/CONTRACT_CREATEFROMDATA* ile ZZ1 (fiyat/indirim kodu vb.) geçirirken `ls_parex-structure = 'BAPE_SDSALESDOC'` + `ls_bape TYPE bape_sdsalesdoc`. BAPE_VBAK yalnız VBELN + /DMBE/* içerir (ZZ1 YOK) → BAPE_VBAK ile "field unknown" aktivasyon hatası; düzeltilip BAPE_VBAK derlense bile S/4 EEW alanları yalnız BAPE_SDSALESDOC üzerinden taşınır → ZZ1 **sessizce kaybolur** (belge kodsuz yaratılır). **Ayrı X yapısı (BAPE_SDSALESDOCX) YOK** AMA X-bayrak BAPE_SDSALESDOC'a GÖMÜLÜ: yapı `key + data(sdsalesdoc_incl_eew_ps) + datax(sdsalesdoc_incl_eew_x)`. **Değer YETMEZ — `datax-<alan> = abap_true` (cfd_custom_change_indicator) DA set edilmeli**, yoksa BAPI değeri UYGULAMAZ → belge kodsuz yaratılır (klasik X-struct mantığı, gömülü). DİKKAT: BAPE_SDSALESDOC geniş → `bapiparex-valuepart1` yalnız 240 char; valuepart1..4'e (yapı char-image'ini DESCRIBE uzunluğuna göre) böl, yoksa truncate/sessiz-kayıp. (ECC'de BAPE_VBAK doğru olabilir; S/4 ≠ ECC.) | HIGH | gate YOK → adt_get `BAPE_SDSALESDOC` (ZZ1 alan + offset) + adt_syntax_check + smoke test-create readback (ZZ1 belgede dolu mu) | ZSD001_P_SOZLESME_KOPYALA 2026-06-26 |
| BE-35 | **Standart-olmayan DDIC char tipi (`TYPE charNNN`)** — DDIC'te yalnız CHAR1..CHAR40 (+ sistem-bağımlı bazı büyükler) data-element olarak VAR; `char150` gibi keyfi büyük tipler TANIMSIZ → aktivasyon `Type CHARnnn is unknown` (type=E). Sabit-uzunluk metin için: `c LENGTH n` (yerel TYPES'te), `string`, veya uygun standart DDIC dtel (`bapi_msg`=CHAR220, `text80`/`text255` vb.). **Aktivasyon-only:** abaplint/run_review DDIC tip-varlığını doğrulamaz → GEÇER; yalnız SAP aktivasyonu yakalar. Build'de `TYPE char<N>` (N>40) gördüğünde tipin varlığını adt_get/syntax_check ile teyit et ya da c LENGTH/dtel kullan. | MEDIUM | heuristik grep `TYPE char[5-9][0-9]\|char[0-9]{3}` → WARN + adt_syntax_check | ZSD001_I_SOZKOP_T01 (char150) 2026-06-26 |
| BE-36 | **ABAP substring `obj+off(len)` — `len` YALNIZ literal VEYA çıplak değişken olabilir; HESAPLI İFADE OLAMAZ** (COND / `lv_len - N` / method-call). Boşluksuz `+480(lv_len - 480)` → `"480( LV_LEN" was expected`; boşluklu `+720( COND… )` → `Method "<OBJ>+720" is unknown` (substring değil method-call parse edilir). DOĞRU: ifadeyi ÖNCE bir değişkene hesapla (`lv_l = lv_len - 480.`), sonra `obj+off(lv_l)`. Literal (`+0(240)`) ve çıplak-değişken (`+0(lv_len)`) GEÇERLİ. **abaplint bunu `parser_error` verir — KÖR FALSE-POSITIVE SAYMA** ([[feedback_abaplint-parser-error-gercek-olabilir]]); SAP aktivasyonu gerçek olduğunu kanıtlar (aktivasyon-only). | MEDIUM | abaplint parser_error (sinyal, yoksay-ma) + adt_syntax_check | ZSD001_I_SOZKOP_C01 2026-06-26 |
| BE-37 | **S/4 key-user (EEW) özel alanları BAPI EXTENSIONIN'e KLASİK ham-yapı-imajıyla GEÇMEZ → `CL_CFD_BAPI_MAPPING` ile serileştirilir (BE-34'ün valuepart char-image kısmını DÜZELTİR).** BE-34 yapı seçimini (BAPE_SDSALESDOC, key/data/datax) doğru saptadı AMA değeri klasik yolla (yapıyı `c`'ye CAST → `valuepart1..4`'e böl, `structure='BAPE_SDSALESDOC'`) geçirmek S/4 EEW'de **sessizce yok sayılır** → belge yaratılır ama ZZ1 alanları BOŞ. Kök-neden (canlı `CL_CFD_BAPI_MAPPING` kaynağı): EEW BAPIPAREX'i (a) yapıyı **XML'e serileştirir** (`transform_to_xml`), (b) `STRUCTURE` alanına ham `'BAPE_SDSALESDOC'` değil **UUID-tabanlı container adı** yazar (`create_uuid_c32`), (c) okuma `cl_abap_matcher` regex + `transform_from_xml`. Klasik ham-imaj bu pattern'e uymaz → satır atlanır; `datax` X-bayrağı ALAKASIZ (tüm girdi tanınmıyor). **DOĞRU:** `cl_cfd_bapi_mapping=>get_instance( )->map_to_bapiparex_single( EXPORTING ir_source_structure = REF #( ls_bape ) CHANGING ct_bapiparex = lt_parex )` — `ls_bape TYPE bape_sdsalesdoc` doldur (header→key BOŞ; data-zz1_* + datax-zz1_*=abap_true), `lt_parex TYPE bapiparextab`, `APPEND LINES OF lt_parex TO ct_ext`, `CATCH cx_cfd_bapi_mapping`. İmzayı (get_instance/map_to_bapiparex_single/REF TO data/bapiparextab) **canlı sınıftan adt_get ile DOĞRULA, UYDURMA**. **RUNTIME-ONLY:** her iki yol da derlenir/aktive olur; yalnız gerçek BAPI-create + **VBAK ZZ1 readback** ortaya çıkarır. EK CAVEAT: contract BAPI'sinin EEW EXTENSIONIN'i gerçekten işlediği readback'siz KANITLANMAZ — boş kalırsa BAPI yetenek-boşluğu → fallback released `API_SALES_CONTRACT_SRV` / `BAPI_CONTRACT_CHANGE` (direkt VBAK UPDATE YASAK, ADR 0005-B). | HIGH | gate YOK → adt_get `CL_CFD_BAPI_MAPPING` imza + adt_syntax_check + **smoke create→VBAK ZZ1 readback** | ZSD001_P_SOZLESME_KOPYALA 2026-06-27 · düzeltir [[BE-34]] |
| BE-38 | **Classic DDIC `define view` (sqlViewName) view-entity-only söz dizimini REDDEDER** — AMDP `USING <sqlview>` + SE16N gerektiğinde view entity'ye geçilemez (DB SQL view yok) → classic `define view` (`@AbapCatalog.sqlViewName`) ZORUNLU; ama classic parser şu view-entity construct'larını reddeder: (a) **`IN (liste)`** → `Unexpected word "in"`; çözüm `fld <> 'a' AND fld <> 'b' AND ...` zinciri. (b) **aggregate (max/min/sum) İÇİNDE CAST** → `Identifiers, literals, and CASE are allowed in aggregate functions`; çözüm CAST'i aggregate DIŞINA (üst view/SELECT listesi — classic'te serbest) taşı VEYA 2-katlı view (iç cast, dış aggregate). (c) **UNION'da `key` YALNIZ 1. dalda**; 2. dal alanları pozisyoneldir → 2. dalda `key` tekrarı parse hatası. (d) `_assoc.field`/`$projection`/`composition`/`define view entity` construct'ları YASAK. **GÜVENLİ (classic-destekli):** `$session.system_language`, SELECT listesinde `cast(.. as abap.char(n))` (aggregate-dışı), searched-CASE, LEFT OUTER + `IS NULL` anti-join, UNION ALL, GROUP BY+max. **Aktivasyon-only:** abaplint/run_review GEÇER; yalnız SAP aktivasyonu yakalar. | MEDIUM | gate YOK → adt_syntax_check + DDL grep (`not in (` · `(max\|min\|sum)\( *cast` · UNION 2.dal `key`) | ZSD001 KASA_MOVEMENT/BEYAN/EXTRE 2026-06-28 |
| BE-39 | **Klasik program include push + PROGRAM-düzeyi aktivasyon → include OBJESİ `version=inactive` kalabilir; program'ın aktif load'u ESKİ include'u kullanır → fix CANLI DEĞİL.** `adt_activate(<program>)` include'ları her zaman co-aktive ETMEZ (özellikle birbirine bağımlı include'larda: C01, T01'in yeni imzasına bağlıysa T01 aktive olmadan C01 inline-aktivasyonu patlar → program-aktivasyon "başarılı" döner ama C01 inactive kalır). Gateway'in `activated=true` + **naive readback'i (source/main = working/inactive sürümü döndürür)** "fix canlı" diye YANILTIR (HTTP-200 ≠ aktif). **DOĞRU VERIFY:** her include için `adtcore:version=active` (metadata) **VE** ACTIVE source'u (`?version=active`, working DEĞİL) oku → fix markerleri orada mı. `scripts/worklist_audit.py` inactive-worklist'i raporlar. **FIX:** include'u explicit `adt_activate` + programı re-activate + active-source readback. **DEF/IMPL SPLIT INCLUDE (T01-def + C01-impl — ör. metot bir include'da DECLARE, diğerinde IMPLEMENT) → hiçbiri TEK BAŞINA aktive olmaz** ("Implementation missing for method X" / def eksik); ⚠ `adt_activate` **include tipini KABUL ETMEZ** (`unsupported_type`) ve `push_object.py` tek-tek aktive eder → çözmez. DOĞRU: tek `/sap/bc/adt/activation?method=activate` POST'unda **HER İKİ include ref'i BİRLİKTE** seed et (atomik co-activation; sap_adt_lib activation-pattern'i kopyala, tahmin YOK). *(Tooling notu — lider: `adt_activate`'e include-pair / worklist-batch aktivasyon yeteneği eklenebilir; tekrar eden desen.)* **RUNTIME-EFFECTIVE gap:** kod doğru, aktive "görünür", ama eski sürüm çalışır. | HIGH | worklist_audit.py + adt_get her-include `version=active` + ACTIVE-source (`?version=active`) readback | ZSD001_I_SOZKOP_C01 2026-06-28 |
| BE-40 | **CDS table function'ı tüketirken `@Environment.systemField: #CLIENT` parametresi (genelde `p_client`) AÇIKÇA GEÇİLEMEZ — derleyici bağlar.** `SELECT FROM ztabfn( p_client = @sy-mandt )` → `The parameter P_CLIENT can only be bound by the compiler.` Client-bağımlı table-function deseninde p_client `@Environment.systemField:#CLIENT` ile işaretlidir → derleyici otomatik aktif client'a bağlar. **FIX:** `SELECT FROM ztabfn( )` (client argümanını ATLA; diğer iş-parametreleri varsa onları geç). **Aktivasyon/program-context-only:** abaplint/run_review/bug-gate statik kaçırır; yalnız program-context `adt_syntax_check`/aktivasyon yakalar. | MEDIUM | gate YOK → program-context adt_syntax_check + grep (table-fn çağrısında `\w+\( *p_client *=`) | ZSD001_I_AMBTAK_F01 2026-06-28 |
| BE-41 | **Klasik `PERFORM <form> USING <aktüel>` — aktüel param DATA-OBJESİ olmalı; string-template (`\|...\|`) / ifade GEÇERSİZ.** `PERFORM html_add USING \|{ ls-x }\|` → `Field "\|" is unknown.` (parser `\|`'i field-adı sanır). Düz `'literal'` ve çıplak değişken/field-symbol GEÇERLİ; yalnız ifade (`\|...\|`, func-call, COND) geçersiz. **FIX:** ifadeyi ÖNCE değişkene al (`DATA(lv) = \|...\|. PERFORM html_add USING lv.`) **VEYA** FORM yerine METHOD kullan (metot aktüel-param olarak ifade KABUL eder → `lo->html_add( \|...\| )`). **Aktivasyon-only:** abaplint/bug-gate statik kaçırır. | MEDIUM | gate YOK → program-context adt_syntax_check + grep (`PERFORM +\w+ +USING +\|`) | ZSD001_I_AMBTAK_F01 2026-06-28 |
| BE-42 | **Sanal element (`@ObjectModel.virtualElement`) + SADL calc-exit (`IF_SADL_EXIT_CALC_ELEMENT_READ`) = statik gate YETMEZ → CANLI doğrula (RUNTIME-ONLY).** JOIN'lenemeyen kaynaktan (STXL/READ_TEXT, hesap) hesaplanmış görüntü kolonu; run_review/syntax/ATC GEÇER, yalnız canlı OData'da çıkar. 3 tuzak: (a) **`get_calculation_info` orig-element adı CASE-SENSITIVE UPPERCASE** (`'ORDERNO'`, camelCase DEĞİL) → aksi `CX_SADL_EXIT_WRONG_ELMENT` → RAISE_SHORTDUMP (OData 500); (b) **`abap.string` CAST'ta geçersiz** → `cast('' as abap.char(N))`, max 1333; (c) **metin OKUMASI: READ_TEXT yerine PROVEN okumayı reuse** (READ_TEXT tdname-format + dil tuzağı: yazıcıyla birebir tdname, sy-langu≠metin-dili → boş) — değer başka ekranda çalışıyorsa o okumayı kopyala (ör. `get_order_texts` RAP `\_Text`, dil-bağımsız). **VERIFY:** canlı OData curl (.conn_adt, `$select=alan`) → 500=dump/boş=okuma-bug; dump kök-neden ADT runtime-dumps feed (`Accept: application/atom+xml;type=feed`); gerçek-key STXH `adt_classrun` probe. Reçete: playbook/adt-cds.md T10. | HIGH | gate YOK (deterministik değil) → reviewer/bug-expert: canlı OData curl + (boşsa) dump-feed/STXH probe ZORUNLU; statik PASS'e güvenme | ZSD001_C_SE_REPORT + ZCL_SD001_SEREP_TEXTS 2026-06-29 |
| BE-43 | **Çok-tipli sevk-emri OPENITEM/OPENHDR `t_setype` join'i: config vkorg+vtweg+auart'ı BAŞKA tiple paylaşıyorsa `spart` ZORUNLU.** `ZSD001_T_SETYPE` bir SE-tipini (se_type×vkorg×vtweg×**spart**×auart) tanımlar. İki tip aynı vkorg+vtweg+auart'ı paylaşıp yalnız SPART'la ayrışabilir (ör. DSK 1100/30/**80**/ZC14 ↔ SIP 1100/30/**20**/ZC14). OPENITEM/OPENHDR'ın t_setype inner-join'i **spart'ı ATLARSA çapraz-kirlenme**: bir tipin picker'ı diğer tipin siparişlerini listeler. **FIX:** join'e `and st.spart = <order-header-spart>` ekle — OPENHDR ile OPENITEM AYNI spart alanını kullanmalı (tutarlılık): vbap+vbak select'te `vbak.spart`; `ZSD001_I_ORDER_ITEMS` üstünde `src.Division` (= vbak.spart, header-level — teyit et). **Aktivasyon GEÇER (syntax doğru), yalnız yanlış-veri olarak çıkar** → statik/syntax gate kaçırır; config canlı okunup (adt_table_read T_SETYPE) vkorg+auart-overlap kontrol edilmeli. Bkz. memory project_zsd001-dsk-fih-se-blok-kontrol. | HIGH | gate YOK → yeni SE-tipinde reviewer/bug-expert: T_SETYPE canlı oku + overlap varsa OPENITEM+OPENHDR spart-completeness doğrula (grep `st\.spart`) | DSK/FIH/IHR OPENITEM 2026-07-03 |
| BE-44 | **`SELECT SUM(...)` (skaler aggregate, FAE'siz) → ham-satır `FOR ALL ENTRIES` + `COLLECT` dönüşümü: SELECT-listesi kaynak tablonun TEKİL-KEY'ini içermezse FAE implicit-DISTINCT toplamı EKSİK sayar (SESSİZ under-count).** ATC Prio-1 "Local Nested Reading DB OP" (loop-içi SELECT) düzeltmesinde loop-içi `SELECT SUM(qty) WHERE key=...` → loop-öncesi toplu okumaya çevrilirken doğru desen "ham satır FAE + in-memory COLLECT". ANCAK `SELECT f1,f2,qty FROM t FOR ALL ENTRIES IN @it ...` sonuç kümesine **projekte kolonlara göre implicit-DISTINCT** uygular → aynı projeksiyona sahip iki FİZİKSEL satır (ör. iki farklı belge/SE AYNI miktar) TEKE iner → COLLECT eksik toplar → save-otoriter validasyon (capacity/over-allocation) SESSİZCE geçer. **FIX:** SELECT-listesine kaynak tablonun TAM tekil-key'ini EKLE (her fiziksel satır tekilleşsin, collapse olmasın) → COLLECT hedefi yine YALNIZ {group-key + qty} (ekstra key kolonları COLLECT VALUE'ya SIZMAMALI, yoksa gruplama parçalanır = ayrı regresyon). Agregat-CDS'ten (zaten grouped/tekil-key) FAE veya key'li lookup FAE bu tuzağa DÜŞMEZ; yalnız ham-satır→COLLECT feed. **run_review/syntax/ATC GEÇER (Prio-1 temizlenir bile), yalnız EŞİT-değer-kolizyon runtime senaryosunda yanlış-sonuç** → bug-expert el-ile-izleme (iki-eşit-miktar → collapse mı) ile yakalar. | HIGH | gate YOK → bug-expert: FAE+COLLECT deseninde SELECT-listesi kaynak-tekil-key içeriyor mu; yoksa eşit-değer kolizyon senaryosunu el-ile izle | ZCL_SD001_FITSE validate_items set-based refactor 2026-07-05 |
| BE-45 | **Bir association'ın ON-condition'ında BAŞKA bir association'ın türevi kullanılamaz (SDDL 515 + 061) — join-key'ler BASE-TABLO alanı olmalı; assoc-türevli alanlar için `$projection` YETMEZ.** İki kademeli aktivasyon-only tuzak: (515) ON'da doğrudan `_OtherAssoc.Field` yazmak → `Association "_X" cannot be used in the on condition of "_Y"`; (061) bunu `$projection.Field`'a çevirmek 515'i çözer AMA eğer o projection-elemanının KENDİSİ assoc-türevliyse (ör. `_OrderItem.Material as Material`) → `The association "_Y" cannot be used locally in the view` (assoc dolaylı olarak hâlâ başka assoc'a bağımlı → lokal dereference yasak). **NEDEN bazı view'da çalışır bazında çalışmaz:** join-key SELECT'te BASE-TABLO alanıysa (ör. `vbap.matnr as Material` — OPENITEM/SE_REPORT) `$projection` düz alana çözülür, sorun YOK; ama key assoc-navigation ise (ör. `_OrderItem.Material` — t_dorit'te matnr yok, SE saved-item ITEM view) 061 kaçınılmaz. **Byte-parity bir precedent'ten kopyalamak grain-kaynağı farklıysa TRANSFER OLMAZ.** **FIX:** (a) join-key'leri base-tablo alanı olarak somutlaştıran ara/alt-view (VBAP'tan direkt matnr/werks) → association O base alanlara $projection ile bağlanır; VEYA (b) association yerine **LEFT OUTER JOIN** kullan (agregat-view'lara; canlı precedent `ZSD001_I_SE_REPORT` — join ON'u from-clause base alanlarına iner). **run_review/abaplint/bug-gate statik GEÇER; yalnız SAP aktivasyonu yakalar** (delta push edilmeden `adt_syntax_check` de göremez → bug-expert "assoc-türevli join-key'e $projection ON" desenini görüp "aktivasyonda 061 çıkabilir" flag'lemeli). | HIGH | gate YOK → bug-expert: association ON'unda kullanılan $projection alanı base-tablo mu assoc-türevli mi (assoc-türevli → 061 riski flag); aktivasyon deterministik teyit | ZSD001_I_SE_A/SE_B_ITEM saved-item stok 2026-07-05 |
| BE-46 | **Klasik user-exit/enhancement include'u ESKİ program bağlamında (fixed-point arithmetic KAPALI — MV45AFZZ / MV50AFZZ ve çoğu SAPMV*/SAPLV*) STRICT/MODERN ABAP SQL + INLINE DECL KABUL ETMEZ.** YASAK: `@`-escaped host var (`@vbak-vkorg`), SELECT-list host-expr VEYA **LİTERAL** (`SELECT SINGLE @abap_true` / `SELECT SINGLE 'X'` — literal de SQL field-expression → strict; varlık kontrolü için GERÇEK KEY KOLONU kullan: `SELECT SINGLE vkorg INTO lv_dummy ... IF sy-subrc = 0`), inline `@DATA(...)` (SQL INTO), `FOR ALL ENTRIES IN @it ... INTO TABLE @DATA(...)`; güvenli tarafta string-template (`\|...\|`) yerine klasik `CONCATENATE`/`WRITE ... TO`. **Aktivasyon hatası:** *"...additions that can only be used when the fixed point arithmetic flag is activated (host variables in expressions)"* + cascade *"Field <X> is unknown"* (SELECT derlenemeyince inline `@DATA` hiç oluşmaz). **NEDEN:** eski program fixed-point OFF → strict ABAP SQL (7.40 `@`-escaping) derlenmez; mevcut çalışan kod klasik olduğu için sorun görünmez (yanıltıcı). **KULLANICI KURALI (2026-07-06):** bu tip exit include'larında **inline decl KULLANMA** — tüm değişkenleri baştan `DATA:` ile bildir + **klasik SQL** (`SELECT SINGLE 'X' FROM t INTO lv WHERE k = struct-f.` — `@` yok). **run_review/abaplint/bug-gate + standalone `adt_syntax_check` HEPSİ GEÇER; yalnız GERÇEK aktivasyon (parent-program derleme) yakalar** — include tek başına context'siz → syntax-check FAKE, gerçek hata AKTİVASYONDA (kullanıcı ayracı 2026-07-06). | HIGH | validator adayı (grep: exit/enhancement include'da `@\w`/`@DATA`/`INTO TABLE @`); bug-expert: user-exit/enhancement include'da strict-SQL/inline-decl FLAG + klasik-SQL öner; kesin teyit parent-program aktivasyonunda | ZSD000_I_SAVE_DOCUMENT_PREPARE + ZSD000_I_50SAVE_DOC_PREPARE 2026-07-06 |
| BE-47 | **ABAP string-template `\|...\|` İÇİNDE literal `\|` kaçırılmamış (`}\|{` deseni)** — alan-birleştirme imzasında ayraç olarak literal pipe kullanınca (`\|{ f1 }\|{ f2 }\|{ f3 }\|`) ortadaki `\|`'ler template'i ERKEN KAPATIR → **SAVE reddi** `ExceptionResourceScanDuringSaveFailure / OO_SOURCE_BASED 011: Unmasked symbol '\|' in string template`. Template içinde literal `\|`/`{`/`}`/`\` sırasıyla **`\\|`/`\{`/`\}`/`\\`** ile kaçırılmalı; yalnız template-açan baştaki `\|{` + kapatan sondaki `}\|` sınır kalır. Semptom grep: `.clas.abap`'te kaçışsız `}\|{`. **abaplint `parser_error` verir — KÖR FALSE-POSITIVE SAYMA** ([[feedback_abaplint-parser-error-gercek-olabilir]] + BE-36); GERÇEK. Fix pipe'ları `\\|`'e çevirir, imza semantiği (ayraç sayı/sıra) korunur. **SAVE-only:** standalone/abaplint parser desync (aşağı bak) yüzünden bazen kaçar → CANLI SAP syntax-check yakalar. | HIGH | abaplint parser_error (sinyal) + **CANLI adt_syntax_check** + grep kaçışsız `}\|{` | ZSD001 EXCUPL 2026-07-12 |
| BE-48 | **`METHODS`/`CLASS-METHODS` parametre-ek SIRASI zorunlu: IMPORTING → EXPORTING → CHANGING → RETURNING → RAISING.** Sıra bozuksa (ör. `EXPORTING`, `CHANGING`'den SONRA) → **SAVE reddi** `OO_SOURCE_BASED 011` (source-based class parse). Named-param çağrılar etkilenmez → implementasyona DOKUNMA, yalnız declaration'da ekleri doğru sıraya diz. RETURNING tek + son (RAISING'den önce). Çok-ekli her metot imzasını tara. **abaplint `parser_error` (line=METHODS satırı) verir — GERÇEK, false-positive sayma** (BE-36). **SAVE-only:** modern-class parser desync bunu gizleyebilir → CANLI SAP syntax-check otoriter. | HIGH | abaplint parser_error (sinyal) + **CANLI adt_syntax_check** | ZSD001 EXCUPL 2026-07-12 |
| BE-49 | **Klasik ALV fieldcatalog `coltext = TEXT-cNN` (text-symbol) → text-pool AYRI OBJEDİR; `adt_push_source`/MCP push_source SADECE source/main taşır, text element'leri TAŞIMAZ.** Yüklenmezse `TEXT-cNN` BOŞ döner → **ALV kolon başlıkları + selection-screen etiketleri + pushbutton text'leri BOŞ görünür** (kaynak doğru + program aktif + çalışır; yalnız RUNTIME'da fark edilir = "activated/aktif ⇒ bitti" tuzağının klasik-ALV hali). "Bitti" kriteri: program `textpool/` klasörü varsa `push_textpool.py` ile symbols+selections YÜKLENMELİ + **canlı `/sap/bc/adt/textelements/programs/<prog>/source/symbols` (version=active) GET ile DOLU doğrulanmalı.** bug-expert: klasik programda fcat `coltext = TEXT-...` VEYA seçim-ekranı/pushbutton text-symbol referansı görürse → text-pool deployment + canlı-teyit KAPSANIYOR mu FLAG. **run_review/abaplint/adt_syntax_check HEPSİ GEÇER (kaynak doğru); yalnız çalıştırınca boş kolon/etiket görülür.** | HIGH | gate YOK → bug-expert statik-flag (fcat `coltext=TEXT-` + repo'da `textpool/` var) + `push_textpool.py` canlı readback | ZSD001 EXCUPL 2026-07-12 |

| BE-50 | **LOOP-içi `DATA` accumulator/flag her iterasyonda SIFIRLANMAZ (declaration hoisting → metot-kapsamlı, bir kez init) → önceki iterasyonun değeri sonrakine SIZAR.** İki varyant: (a) `DATA lt TYPE string_table.` (veya iç tablo/WA) LOOP içinde + `APPEND` var ama iterasyon-başı `CLEAR` YOK → satır N'in içeriği N+1..'e **birikir**; (b) `DATA flag TYPE abap_bool VALUE abap_false.` LOOP içinde → `VALUE` yalnız 1 kez atanır, iterasyon-başı reset YOK → erken iterasyonun bayrağı sonrakine **stale** kalır. Semptom: çok-satır/çok-grup işlemede bir kaydın hatası/durumu diğerlerine yayılır (EXCUPL: tek geçersiz müşteri → ÜST tabloda TÜM başlıklar "müşteri tanımlı değil"; match_existing lv_dup_match/lv_unit_err stale → yanlış UPDATE sınıflaması). **FIX:** her iterasyon başında `CLEAR: <accumulator/flag>.`. **abaplint/adt_syntax_check GEÇER; yalnız çok-satır runtime senaryosunda yanlış-sonuç** → bug-expert her LOOP'ta DATA-tanımlı accumulator/flag'ın per-iterasyon CLEAR'ını izler. | HIGH | gate YOK → bug-expert statik-izleme (LOOP içi `DATA`/`DATA..VALUE` accumulator/flag + CLEAR yok) | ZSD001 EXCUPL validate lt_err (kullanıcı GUI) + match_existing lv_ 2026-07-13 |
| BE-51 | **Değişkeni `DATA` bildiriminden ÖNCE (textual sıra) kullanmak → SAP kernel `Field "X" is unknown` (aktivasyon/SAVE reddi); abaplint `DATA`'yı HOIST edip GEÇER (kör nokta).** Klasik tuzak: `CLEAR: a, b, lt_err.` satırı `DATA lt_err TYPE string_table.` satırından ÖNCE. abaplint scope-hoisting yapar → "temiz" der; SAP kernel textual use-before-declare'i reddeder (`OO_SOURCE_BASED` / activation type=E). **FIX:** tüm `DATA` bildirimlerini o değişkenin İLK kullanımından ÖNCE koy (LOOP başında). **CANLI adt_syntax_check yakalar, abaplint/bug-gate KAÇIRIR** — BE-47/48/66 ile aynı aile (modern/source-based class'ta abaplint ≠ SAP). | MEDIUM | gate YOK → **CANLI adt_syntax_check** (gateway) + grep (`CLEAR`/kullanım, `DATA <x>` sonrası) | ZSD001 EXCUPL validate lt_err reorder 2026-07-13 |
| BE-52 | **UNIT-tipli alanda (VRKME/MEINS) DIŞ birim kodu saklayıp göstermek → `BM302 internal unit … not maintained in TR` + ALV kolonu `******`.** ALV/`WRITE`, UNIT-tipli alana **CUNIT output-conversion** uygular: alandaki değeri **İÇ MSEHI** sayıp dile göre dış koda çevirir. Excel/EDI'den gelen dış kod (Türkçe 'ADT' = iç 'ST'nin MSEH3'ü) bir UNIT-alanında tutulursa → CUNIT 'ADT'yi iç birim sanar, T006'da bulamaz → BM302 + `******`. **AYNI KÖK-NEDEN SAVE tarafında da:** RAP/EML/DB **İÇ MSEHI** bekler → dış kod ('ADT') EML'e giderse `MODIFY` GEÇSE BİLE **COMMIT/save REDDEDİLİR** (çoğu zaman generic "kaydetme başarısız" → gerçek neden GİZLİ; canlı ders: Excel 'ADT'→'ST' çevrilmeden save-fail günlerce teşhis edilemedi). Yani dış-kod-iç-kod ayrımı hem **save** hem **gösterim**i vurur. **KURAL:** UNIT-alanında **DAİMA İÇ MSEHI** sakla ('ST'); ALV/CUNIT dış kodu ('ADT') dile göre **otomatik** gösterir + miktar ondalığı (T006-DECAN) doğru gelir. Dış→iç çevrim girişte `CONVERSION_EXIT_CUNIT_INPUT` ile **bir kez**; dış kodu ayrı gösterim-alanında **TUTMA** (çift unit-alanı gereksiz + `******` kaynağı — repo standart deseni: tek iç-birim alanı). **run_review/syntax/abaplint GEÇER; yalnız çalıştırınca ****** / BM302** → bug-expert: UNIT-alanına dış-kod atama VEYA iki-unit-alanı (dış+iç) desenini flag. | HIGH | gate YOK → bug-expert statik-flag (UNIT-alanı populate'i CUNIT_INPUT'tan mı; çift-unit-alanı var mı) + canlı ALV | ZSD001 EXCUPL vrkme/vrkme_int ****** 2026-07-13 |

| BE-53 | **RAP/EML tüketiminde (klasik/report bağlamı) reported mesajlarını GENERIC'e indirgeme — gerçek hata mesajını yüzeye çıkar.** `MODIFY`/`COMMIT ENTITIES` FAILED/REPORTED döndüğünde `collect_reported`'ı tek generic'e ("Kaydetme başarısız") çevirmek → kullanıcı gerçek nedeni (birim/partner/eksik-alan) GÖREMEZ, teşhis imkânsızlaşır (canlı ders: "save failed" tek-satır günlerce kök-neden gizledi). **KURAL:** reported'tan **error-type (E/A/X) TÜM mesajları** topla (BAPIRET2 / `if_t100_message`→get_text; boşsa ham `msgid msgno`), ekrana/log'a **hepsini** ver (çok-mesaj → popup + 100-char birleştir); non-error (S/I/W) verbatim gösterme; reported tamamen boşsa son-çare generic. **EARLY(MODIFY) + LATE(COMMIT) reported AYRI TİP** → ayrı topla (`TYPE any` + dinamik bileşen). bug-expert: EML tüketiminde reported→tek-generic collapse VEYA yalnız-ilk-mesaj görürse flag. | MEDIUM | gate YOK → bug-expert (collect_reported generic-collapse deseni) | ZSD001 EXCUPL create/update mesaj-görünürlük 2026-07-13 |
| BE-55 | **Metot/interface arayüzünde inline `TYPE STANDARD TABLE OF <tip>` (anonim tablo-tip) GEÇERSİZ — isimli tablo-tip ZORUNLU.** `METHODS m EXPORTING et TYPE STANDARD TABLE OF edi_dd40` → SAP **save-anı resource-scan** reddi (`OO_SOURCE_BASED 011` / `ExceptionResourceScanDuringSaveFailure`; SE24-Check `Unable to interpret <satır-tipi>`). `TYPE STANDARD TABLE OF x` yalnız `DATA`/`TYPES`'te legal; metot/FM/FORM arayüzü **isimli tip** ister → `TYPES tt_x TYPE STANDARD TABLE OF <t> WITH DEFAULT KEY.` tanımla, param `TYPE tt_x` ver (çağıranın yerel default-key tablosu uyumlu). BE-22/BE-23 kuzeni; **BE-47/BE-48 ile aynı save-scan ailesi** (upload-anı, aktivasyondan ÖNCE — inactive'e hiç yazılamaz). **abaplint IŞKALAR** (repo abaplint sözdizim-kontrolü kapalı + DDIC yok → satır-tipini çözemez — KANITLANDI 2026-07-13); SE38 program FORM/DATA'da serbest ("class'ta farklı" tuzağı). Gateway `push_object` bunu **upload-anında** `source_uploaded:false` + `ok:false` ile yüzeye çıkarır (activate'e hiç gelmez; kanıt: guard-test 2026-07-13 save-scan upload'ı reddetti). | HIGH | SAP save-scan (OO_SOURCE_BASED 011) upload-time → push `source_uploaded:false`; abaplint IŞKALAR | inline-TABLE-OF metot-param 2026-07-13 |
| BE-54 | **`cwidth_opt` (LVC layout colwidth-optimize) YALNIZ `set_table_for_first_display`'de GÖRÜNEN veriye göre optimize eder; sonraki `refresh_table_display` RE-OPTIMIZE ETMEZ.** Grid BOŞ açılıp (first_display boş outtab) veri sonradan `fill+refresh` ediliyorsa → kolonlar yalnız BAŞLIK-enine kalır (içerik sığmaz = kısa kolon). **FIX:** first_display'i **veri gelene kadar ERTELE** (ilk dolu fill'de first_display; sonrakiler refresh) — `mv_shown` bayrağıyla; ⚠ first_display'den ÖNCE `refresh_table_display` çağırma (CNTL_ERROR / grid-not-init dump). `cwidth_opt='X'` + manuel `outputlen` birlikte → outputlen pinler, optimize ezilir (data-kolon outputlen'ini SIFIRLA). Zayıf alternatif (sürüm-bağımlı): fill sonrası `set_frontend_layout`+refresh. **run_review/syntax GEÇER; yalnız çalıştırınca kısa kolon** → bug-expert: boş-first_display + sonradan-fill deseninde deferred-first_display/re-optimize var mı flag. | MEDIUM | gate YOK → bug-expert (boş-grid first_display + fill/refresh; outputlen vs cwidth_opt) | ZSD001 EXCUPL alt-grid oto-genişlik 2026-07-13 |

> **MODERN-SYNTAX (EML/RAP/source-based) CLASS → abaplint PARSER DESYNC → CANLI SYNTAX-CHECK ZORUNLU (BE-47/BE-48 dersi 2026-07-12):** abaplint, EML (`MODIFY/READ/COMMIT ENTITIES`, `CONVERT KEY`) ve source-based modern söz dizimini tam parse EDEMEZ → parser **desync** olur; bu, İLERİDEKİ GERÇEK hataları (string-template escape BE-47, METHODS param sırası BE-48) **gizler veya yanlış satırda gösterir**. "abaplint temiz" ya da bug-expert "PASS/bit-identical" bir modern-syntax class'ın SAP'de **DERLENDİĞİNİ KANITLAMAZ.** **KURAL (MUST):** EML/RAP/source-based class `BUG_GATE_READY` denmeden ÖNCE **CANLI SAP syntax-check** yapılır (gateway diag-push: geçici obje ya da inactive push + `adt_syntax_check`) — abaplint/standalone TEK BAŞINA yetmez. Kanıt: ZSD001 EXCUPL'de iki bug (pipe BE-47 + param-sırası BE-48) aynı class'ta ard arda tam bu yolla kaçtı; her biri ancak canlı SAP save/syntax-scan'de yakalandı. Enforcement: bu not + BE-47/BE-48 checklist üyeliği + gateway workflow adımı (yeni gate YOK — moratoryum: canlı-check zaten gateway'in elinde). **GÜNCELLEME 2026-07-13:** gateway `push_object` artık **class/interface** push'unda upload-sonrası/activate-öncesi **otomatik kernel-preaudit** koşar (`sap_client.py`) → **aktivasyon-only** aile (BE-21..25/BE-51) + aktivasyon sahte-OK (BE-39) aktivasyondan ÖNCE, temiz structured hatayla yakalanır; kanıtlandı 2026-07-13 (tanımsız-değişken testi: `source_uploaded:true` + `syntax_precheck:failed`). prog/fugr/include DIŞLANDI (standalone include preaudit FAKE — BE-46). **Save-scan ailesi (BE-47/48/55) bundan ÖNCE, upload-anında düşer** (guard'a gelmez).
>
> **AKTİVASYON-ONLY → RUNTIME-ONLY genişlemesi (BE-26):** BE-21..25 aktivasyonda çıkar; BE-26 aktivasyonda DA çıkmaz (kod sözdizimsel doğru, aktive olur) — yalnız **gerçek OData create/action testinde** dump eder. O yüzden BE-26 deterministik gate (`check_no_rap_commit`) ile yazım-anında bloklanır; bug-expert ayrıca FM-içi-commit (i_opt_commit/RAP'ten direkt commit-BAPI) WARN'ını semantik teyit eder.

> **AKTİVASYON-ONLY uyarısı (BE-21..25):** Bu beş tuzak run_review/abaplint'i GEÇER, yalnız SAP `bactivate`/`adt_syntax_check`'te çıkar (çünkü RAP-context + released-obje + sistem-yetenek gerektirir). Yerel build'de "run_review PASS" ≠ aktive olur. Build'de obje SAP'de yoksa: ÖNCE bu beşini elle tara (özellikle RAP behavior pool yazarken). | HIGH | — | — |

> **TİP:** Her madde **HATA** (kod/obje yanlış) veya **EKSİK** (çalışıyor ama must-do karşılanmamış — ör. BE-11 audit-fill, BE-18 recon-implemente-değil) tipindedir → Bug_Expert `[HATA]`/`[EKSİK]` etiketler; ikisi de zorunlu. Checklist-DIŞI fikir = `[ÖNERİ]` (bağlayıcı değil).
> **Pre-existing AYRI:** değişimle ilgisiz kritik = ayrı işaretle, bloklama. **Tahmin YASAK:** her bulgu canlı obje/syntax/readback ile doğrulanır.
