---
layer: L4
scope: package:{PKG_FULL}
last-updated: {DATE}
status: active
---

# {PKG_FULL} — Paket Kuralları

## Naming (zorunlu prefix'ler)

| Obje Tipi | Prefix | Regex |
|---|---|---|
| Program | `{PKG}_P_*` | `^{PKG}_P_[A-Z0-9_]+$` |
| Include | `{PKG}_I_*` | `^{PKG}_I_[A-Z0-9_]+$` |
| Class | `{PKG}_CL_*` veya `ZCL_{PKG}_*` | `^({PKG}_CL_\|ZCL_{PKG}_)[A-Z0-9_]+$` |
| Function Group | `{PKG}_FG_*` | `^{PKG}_FG_[A-Z0-9_]+$` |
| Function Module | `{PKG}_FM_*` | `^{PKG}_FM_[A-Z0-9_]+$` |
| CDS Root View | `{PKG}_C_*` | `^{PKG}_C_[A-Z0-9_]+$` |
| CDS Interface View | `{PKG}_I_*` | `^{PKG}_I_[A-Z0-9_]+$` |
| DDL Source | `{PKG}_DDL_*` | `^{PKG}_DDL_[A-Z0-9_]+$` |
| Data Element | `{PKG}_E_*` | `^{PKG}_E_[A-Z0-9_]+$` |
| Domain | `{PKG}_D_*` | `^{PKG}_D_[A-Z0-9_]+$` |
| Structure | `{PKG}_S_*` | `^{PKG}_S_[A-Z0-9_]+$` |
| Table | `{PKG}_T_*` | `^{PKG}_T_[A-Z0-9_]+$` |
| Table Type | `{PKG}_TT_*` | `^{PKG}_TT_[A-Z0-9_]+$` |
| Lock Object | `E{PKG}_*` veya `EZ_{PKG}_*` | (lock obj prefix `E` ile başlar) |
| Message Class | `{PKG}_MSG` veya `{PKG}` (legacy) | `^{PKG}(_MSG)?$` |

> Validator: `scripts/validators/check_package_naming.py` bu regex'leri okuyup `cds/`, `classes/`, `programs/`, vb. altındaki dosya adlarını doğrular.

> ⚠ **AÇIK KARAR — `Class` satırının 2. biçimi (ölçüldü 2026-08-29, karar BEKLİYOR).**
> ⓘ Bu not 2026-08-29'a kadar **iki** ekseni taşıyordu. **Message Class ekseni KAPANDI**
> (aşağıdaki ✅ kaydı). Geriye **Class** ekseni kaldı; açık kalma sebebi bir tercih değil,
> **şablon dilinin bu değeri ifade edememesidir**.
>
> | | Message Class | Class (2. biçim) |
> |---|---|---|
> | **Bu şablon** | `{PKG}_MSG` veya `{PKG}` ✅ hizalandı | `ZCL_{PKG}_*` (ör. `ZCL_ZSD001_*`) |
> | **`standards/01-naming.md`** | §4.4.5 `MSG` ✅ hizalandı | §4.4.3 notu: **`ZCL_<PKG#>_*`** |
> | **Canlı kullanım** | `{PKG}_MSG` (yeni) · `{PKG}` (legacy) | `ZCL_<PKG'nin Z'siz hâli>_*` (ör. `ZCL_SD001_*`) |
>
> **Ölçüm — Class (bu core'u tüketen proje ağaçları, salt-okur):** 64 `*.clas.abap`
> artefaktı: `{PKG}_CL_*` **25 dosya / 14 paket**, `ZCL_<Z'siz>_*` **39 dosya / 6 paket**,
> şablonun `ZCL_{PKG}_*` biçimi **0 dosya / 0 paket** (bugüne dek HİÇ kullanılmamış).
> ⚠ `core/` **paylaşılandır**; bu sayılar **bu makinedeki proje ağaçlarıyla** sınırlıdır.
>
> ⭐ **YENİ KANIT (ölçüldü 2026-08-29) — standart bu eksende ZATEN canlı desenle aynı şeyi
> söylüyor:** `standards/01-naming.md` §4.4.3'ün altındaki not ve `standards/05-coding-rap.md`
> §4 **`ZCL_<PKG#>_<ad>`** (ör. `ZCL_SD001_ORDER`) diyor **ve** `{PKG}_CL_*` desenini açıkça
> **LEGACY** ilan ediyor. ⇒ Bu eksende sapan taraf **standart değil, bu şablondur**.
>
> **Neden bu turda düzeltilmedi — YER TUTUCU YOK:** `ZCL_SD001_*` için `{PKG}`'nin **baştaki
> Z'si atılmış** hâli gerekir. `scripts/bootstrap_package.py`'nin yer-tutucu haritası ölçüldü:
> `PKG` · `PKG_FULL` · `TITLE` · `MODULE` · `DATE` · `OWNER` — bu değeri üreten yer tutucu
> **yok** ve `PKG = pkg_full.replace("_CLC","")` (ör. `ZSD001`). Şablon dili bunu **ifade
> edemez**; uydurmak yerine sınır yazıldı. Çözüm: `bootstrap_package.py`'ye `PKG_NOZ`
> eklemek, sonra bu satırı `ZCL_{PKG_NOZ}_*` yapmak (o dosya 2026-08-29 turunun kapsamı
> DIŞINDAYDI — sahibi başka bir parttı).
>
> **O gün gelene kadar ne yapılmalı:** paket gerçekten `ZCL_<Z'siz>_*` kullanacaksa yukarıdaki
> tabloya **AYRI bir satır** ekle (canlı emsal: `| RAP Behavior Class | ``ZCL_SD001_*`` |
> ``^ZCL_SD001_[A-Z0-9_]+$`` |`). Validator aynı klasöre eşlenen TÜM regex'leri **VEYA**'lar,
> yani ek satır mevcut `Class` satırını bozmaz.

> ✅ **KAPANDI — `Message Class` satırı `{PKG}` → `{PKG}_MSG` (2026-08-29, kullanıcı kararı).**
> Bu satır 2026-08-29'a kadar çıplak `{PKG}` yazıyordu, `standards/01-naming.md` §4.4.5 ise
> `MC` diyordu, canlı kullanım ise üçüncü bir şeydi — **üç kaynak üç ayrı şey**. Karar sırası
> **standart → şablon → kapı** olarak **atomik** yürütüldü (biri eksik kalırsa proje kapısı
> `HARITA-SAPMASI` üretir).
> **Ölçüm (bir proje ağacı, salt-okur, 2026-08-29):** `_MC` biçimi **0** atıf. `_MSG` biçimi
> **6 sınıf / 319 atıf**. 23 paketin `.rules.md`'sinin **10**'unda satır var: **5** çıplak
> `{PKG}` (4'ü bu şablonun cümlesini *birebir* miras almış — seçim değil **miras**), **4**
> `{PKG}_MSG`, **1** `{PKG}_*`; 13'ünde satır yok. Gerçek ABAP kodunda (`MESSAGE nnn(...)`)
> **2 sınıf `_MSG`** (17 + 9 atıf) ve **2 sınıf çıplak** (26 ·
> 2 atıf) — yani çıplak biçim **canlıda gerçekten vardır**.
> ⇒ Satır **iki biçimi de** taşır: `{PKG}_MSG` **yeni** objenin biçimidir, çıplak `{PKG}`
> **legacy olarak geçerlidir** (canlı obje var, rename YOK). Tek biçime indirmek mevcut
> o iki çıplak sınıfı sebepsiz ihlale düşürürdü.

> ⛔ **DTEL/Domain öneki `_E_` / `_D_`'DİR — `_DE_`/`_DTEL_`/`_DOM_` DEĞİL** (2026-08-20'de
> düzeltildi). Kaynak: `core/standards/01-naming.md` §4.4.5 → Data Element = **`E`**
> (`ZMM001_E_AMOUNT`), Domain = **`D`** (`ZSD001_D_AMOUNT`). Domain ile DTEL **aynı
> kısaltmayı paylaşmaz**, ayrı harflerdir; canlı sistemde `_DOM_`/`_DE_` önekli **tek bir
> obje yok** (ölçüldü 2026-08-17).
> ⚠ **NEDEN ŞERH DÜŞÜLDÜ:** bu satır şablonda üç yıl yanlış durdu ve **her yeni paket onu
> miras aldı**; aynı düzeltme üç ayrı pakette **ELLE** yapıldı (2026-05-15 · 2026-08-05 ·
> 2026-08-17) — üç turda üç kez elle düzeltmek, düzeltmenin **yanlış katmanda** yapıldığının
> kanıtıdır. Kural dosyası validator'ün GİRDİSİDİR: yanlış olduğunda standarda uygun ad yazan
> geliştirici **validator hatası** alır ve ya kural bükülür ya ad bozulur.

> **Include alt-kuralı — klasik program (std 06 §1 · gate C-INC-NAME-01):** Klasik report/module-pool **tek-body YAZILMAZ**; main = `INCLUDE` + event blokları. Include adı programdan TÜRER: `{PKG}_P_<PRG>` → `{PKG}_I_<PRG>_<Tip><NN>` — Tip: **T**op·**C**ls·**O**=PBO·**I**=PAI·**F**orm·**S**el; NN=01… Örn `{PKG}_P_SIPARIS` → `{PKG}_I_SIPARIS_T01`/`_C01`/`_F01`. Generic `{PKG}_I_TOP` (program-kökü + numaralı suffix yok) YASAK. Include = PROG/I objesi, `programs/includes/`.

## Bağımlılıklar

- Üst paket: TBD (varsa)
- Bağımlı paket(ler): TBD (varsa)

## Bilinen İstisnalar / Legacy

(Varsa burada listele — örn. "Bu pakette FI namespace CDS de var: `ZFI_I_*`")

## Konvansiyon — Legacy/Draft .txt (genel)

Paket root'unda `*.txt` dosyaları **ilk draft spec** olarak tutulabilir:
- Eski sistemden alınan veya manuel yazılmış kod kaynağı
- Yeni sistemde **henüz implemente edilmemiş** veya implementasyona kaynak olmuş
- `.abap` / `.cds` dosyalarına çevrilmez (rename veya silinmez)
- Validator: paket root'undaki `.txt` dosyaları naming regex'inden muaf
- `docs/` altındaki `.txt`'ler ise FS/TS dokümanı (farklı kategori)

## Transport

- Aktif TR: TBD
- Önceki TR'ler: TBD

## İlgili

- [`SPEC.md`](SPEC.md)
- [`SESSION_NOTES.md`](SESSION_NOTES.md)
- [`../../standards/01-naming.md`](../../standards/01-naming.md) — Kurumsal naming standardı
- [`../../governance/package-registry.md`](../../governance/package-registry.md)
