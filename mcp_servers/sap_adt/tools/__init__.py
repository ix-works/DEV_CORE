"""SAP ADT MCP tool modules.

Tool registration happens at import time via @mcp.tool() decorators in:
- atom.py     — 4 atomic ADT operations
- composite.py — 3 composite create flows
- query.py    — 3 read-only query tools
"""
